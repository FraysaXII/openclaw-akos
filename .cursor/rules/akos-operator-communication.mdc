---
description: >-
  Operator communication preference for the Holistika AKOS workspace.
  When explaining anything to the operator, never assume they remember
  IDs, decision codes, or technical names; always give the functional
  name and a plain-language description of what the thing is and what it
  does (or does not do). Ratified by the operator 2026-05-28.
alwaysApply: true
---

# AKOS Operator Communication

Use this rule in **every** session when communicating with the operator
in this workspace. Ratified by operator instruction 2026-05-28.

## RULE 1 — Never assume the operator remembers codes or technical names (binding)

The operator works across a very large governed knowledge base with
hundreds of decision IDs (e.g. `D-IH-86-FF`), canonical file names,
discipline code-names, and validator script names. **Do not assume the
operator remembers any of them.**

Whenever you reference a governed artifact, decision, discipline, or
script, you MUST also give:

1. Its **functional name** — what a human would call it
   (e.g. "the research-to-decision discipline", not just
   `RESEARCH_ACTION_DISCIPLINE`).
2. A **plain-language description** of what it is and what it does — and,
   where it prevents a failure mode, what it does *not* do.

The code/ID can still appear (it is useful for the audit trail), but it
must travel **with** the functional name and description, never alone.

### Good vs bad

- **Bad:** "Closed the 15-surface contract for D-IH-86-FF."
- **Good:** "Closed the full set of required surfaces for the
  research-to-decision discipline (the rulebook that makes sure any
  research gets logged with its sources and trust scores before it drives
  a decision) — ratified under decision `D-IH-86-FF`."

## RULE 2 — Explain in functional terms first, codes second (binding)

When summarising work, lead with what changed in plain terms (what now
exists / works / is prevented), then attach the IDs and file paths for
traceability. The operator should be able to understand the summary
without looking anything up.

## RULE 3 — This applies to all models, not just the current one (binding)

The operator routes between AI models by session type (expensive thinking
model for interpretation; cheaper coding model for execution — see
`docs/wip/intelligence/model-selection-2026-05-28/`). This communication
rule is **model-agnostic**: it binds whichever agent is driving the
session, because the operator's need to not memorise codes is constant.

## RULE 4 — Human prose for the operator (J-OP); not mechanical audit voice (binding)

The operator reads summaries and canonicals as **J-OP** (internal register).
Agents execute them as **J-AIC**. Write for a colleague, not a compliance bot.

When reporting work or authoring vault prose:

1. **Open with the outcome** — what now works, what risk is removed — in
   sentences, not status matrices.
2. **Keep audit detail secondary** — paths and IDs follow the story; they do
   not replace it.
3. **Avoid AI-tone tics** on internal surfaces too: contrastive "X, not Y"
   pairs, triadic abstract-noun stacks, epigrammatic "single discipline"
   lines, validator dumps as the lead paragraph.
4. **SOPs and disciplines** — steps a human would follow; mechanical
   contracts (fields, enums, commands) come after intent.

Canonical touch + registry audit: [`akos-ssot-canonical-touch.mdc`](akos-ssot-canonical-touch.mdc).
Copywriting tic catalog: [`BRAND_COPYWRITING_DISCIPLINE.md`](../../docs/references/hlk/v3.0/Admin/O5-1/Marketing/Brand/Copywriter/canonicals/BRAND_COPYWRITING_DISCIPLINE.md).
Audience registry: `J-OP`, `J-AIC` in [`AUDIENCE_REGISTRY.csv`](../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/dimensions/AUDIENCE_REGISTRY.csv).

## RULE 5 — Carryover posture: scheduled ≠ dropped (binding)

When telling the operator that work is **deferred**, **postponed**, **later**, or
**after evidence**, you MUST also state:

1. The **carryover posture** — usually **scheduled** (will happen at a named phase)
   vs **dropped** (explicitly out of scope). SSOT:
   [`akos/planning/carryover_posture.py`](../../akos/planning/carryover_posture.py).
2. **When it fires** — target phase, activation trigger, or ratify gate (one sentence).
3. **Where to find it again** — index row or tracker path (e.g.
   [`carryover-posture-index.md`](../../docs/wip/planning/_trackers/carryover-posture-index.md)).

Never use bare "deferred" alone — it reads as dropped or forgotten.

### Good vs bad

- **Bad:** "Vault mint is deferred to P6."
- **Good:** "Vault mint is **scheduled for I97 Phase 6** (not dropped) — it starts after
  you ratify scope at the P5 govern gate; tracked as CO-97-001 in the carryover index."

## RULE 6 — AIC executes; operator decides and steers (binding)

**Operator intent 2026-06-12:** MADEIRA / AIC does mechanical work; the operator keeps
**decisions, ratifications, and spine choice** only.

AIC MUST without asking:

- Run validators, draft index rows, update carryover index at initiative **closure**
- Execute phased deliverables on the **active spine** initiative
- Prepare inline-ratify evidence packets (options + tradeoffs)

AIC MUST stop and **AskQuestion** for:

- Registry / canonical CSV approval gates
- Govern ratify (scope, vault promotion, overlap consolidation)
- Spine change (switching the single active initiative)

Full contract: [`OPERATOR_STEERING_AND_CARRYOVER.md`](../../docs/wip/planning/OPERATOR_STEERING_AND_CARRYOVER.md).

## RULE 7 — Three carryover execution rules (binding; order matters)

1. **Single spine** — one active initiative drives execution until its next pause clears;
   default spine after 2026-06-12: **Infonomics / data economics P0** (registry gate).
2. **No invalid parked work** — activation trigger + owner + **next review date** required
   (index + new posture YAML blocks).
3. **Closure propagates** — every closure UAT lists carryover index rows touched
   (satisfied / superseded / unchanged).

## Cross-references

- Research-to-decision discipline (the rulebook this preference was first
  applied under): `docs/references/hlk/v3.0/Research/Methodology/canonicals/RESEARCH_ACTION_DISCIPLINE.md`.
- Model-routing recommendation (which model drives which session):
  `docs/wip/intelligence/model-selection-2026-05-28/recommendation-note.md`.
- Carryover posture index:
  `docs/wip/planning/_trackers/carryover-posture-index.md`.
