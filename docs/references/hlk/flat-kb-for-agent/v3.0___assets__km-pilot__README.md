# km-pilot — Output 1 pilot bundle

Canonical governed visuals for KM pipeline validation. See [v3.0/index.md](../../index.md).

Placeholders duplicate committed brand assets where no Excalidraw export existed in-repo; replace files in place while preserving `source_id` and updating `file_sha256` in each manifest.
