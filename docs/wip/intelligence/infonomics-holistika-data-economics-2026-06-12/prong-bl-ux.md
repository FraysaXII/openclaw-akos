---
intellectual_kind: research_prong
prong: BL-UX
topic_cluster: area-maturity-bar
parent_pack: infonomics-holistika-data-economics-2026-06-12
authored: 2026-06-13
status: active
language: en
downstream_decision: D-INF-ECON
linked_research_sources:
  - SRC-INF-INT-050
  - SRC-INF-EXT-474
  - SRC-INF-EXT-475
  - SRC-INF-EXT-488
  - SRC-INF-EXT-489
---

# Prong BL-UX — Marketing / Brand UX (insight-machine & operator-journey economics)

> Per [`SOP-RESEARCH_PRONG_SYNTHESIS_001.md`](../../../references/hlk/v3.0/Research/Methodology/canonicals/SOP-RESEARCH_PRONG_SYNTHESIS_001.md). Feeds **D-INF-ECON**: value of operator-facing insight surfaces vs cognitive/load cost.

## Header

| Field | Value |
|:---|:---|
| Baseline prong | `BL-UX` — Marketing / Brand UX consumer |
| Ledger rows | **10** cumulative (4 CORPINT + 6 OSINT) — **thin prong; acknowledge explicitly** |
| Skeptic / tradeoff voices | **2** rows (`SRC-INF-EXT-488`, `SRC-INF-EXT-489`) |
| Downstream decision | **D-INF-ECON** — price governed analytics UX without dashboard vanity metrics |

## Narrative findings

### U.1 Thin ledger is honest: UX economics anchor on four CORPINT pillars

Only four internal sources: UX_DISCIPLINE (`SRC-INF-INT-050`), BRAND UX DESIGNER CHARTER (`SRC-INF-INT-014`), BRAND GANTT DISCIPLINE (`SRC-INF-INT-049`), and gantt confidence validator (`SRC-INF-INT-187`). Quality Fabric compose_UX forward charter lives here — Infonomics must value **multi-axis UX bar** (audience × channel × scenario × brand × governance), not page count. Additional UX economics evidence sits on BL-MKT (brand surfaces) and I96 Research Center UAT packs (`SRC-INF-EXT-474`–`476` harvested from governed-operator-journey work).

### U.2 Governed operator journey OSINT defines audit-grade disclosure patterns

GOV.UK summary list (`SRC-INF-EXT-474`) supports key-value audit tables; IxDF progressive disclosure (`SRC-INF-EXT-475`) limits cognitive layers; Carbon shell header (`SRC-INF-EXT-476`) patterns POV switcher + freshness strip from Research Center v2 UAT. These are **load-bearing** for Infonomics insight machines: economic value = faster operator ratify, not prettier charts.

### U.3 Skeptics block analytics-as-truth capitalization

NN/g attribution limits (`SRC-INF-EXT-488`) and HBR "don't just follow the data" (`SRC-INF-EXT-489`) argue dashboards overstate decision value. Aligns with UX_DISCIPLINE (`SRC-INF-INT-050`) — external register surfaces must not imply precision Holistika internal grades do not support.

### U.4 Gantt confidence validator ties UX schedule claims to measurable confidence

`SRC-INF-INT-187` (`validate_brand_gantt_confidence.py`) mechanizes timeline credibility — Infonomics can attach **confidence-adjusted NPV** to UX-led initiatives (I96 panels, Research Center tranches) instead of raw story points.

### U.5 Cross-prong dependency: actionable analytics definition on BL-DATA

Gartner actionable analytics (`SRC-INF-EXT-464` on BL-DATA) defines decision-ready insight — BL-UX implements the shell; BL-DATA owns data product semantics. D-INF-ECON must not value UX without linked consumer registry row (BL-DATA `BI_CONSUMER_REGISTRY`).

## PESTEL — six viewpoints

| Letter | Viewpoint | Finding (cite `SRC-*`) |
|:---|:---|:---|
| **P** | Political / regulatory | GOV.UK patterns (`SRC-INF-EXT-474`) reflect public-sector transparency — internal ERP UX inherits audit-table expectation for operator sign-off. |
| **E** | Economic | Dashboard anti-patterns NN/g (`SRC-INF-EXT-487`) — build cost vs decision uplift; skeptic HBR (`SRC-INF-EXT-489`) caps ROI claims. |
| **S** | Social | Progressive disclosure (`SRC-INF-EXT-475`) — socialises cognitive load limits for operators juggling multi-initiative spines. |
| **T** | Technological | Carbon shell (`SRC-INF-EXT-476`) — design-system reuse lowers marginal screen cost; validator (`SRC-INF-INT-187`) reduces schedule lie risk. |
| **E** | Environmental | Fewer layers = less render/telemetry waste — UX simplification is green IT Infonomics. |
| **L** | Legal | UX_DISCIPLINE (`SRC-INF-INT-050`) + brand dual-register — misleading external analytics create legal/reputational liability. |

## Porter — four forces + competition synthesis

| Force | Finding (cite `SRC-*`) |
|:---|:---|
| **Supplier power** | Design system vendors (Carbon ref `SRC-INF-EXT-476`), Figma registry (BL-MKT) — template power over build cost. |
| **Buyer power** | Operators demand ratify-ready drawers/accordions (Research Center UAT lineage) — reject vanity dashboards (`SRC-INF-EXT-489`). |
| **Threat of substitutes** | Raw CSV + grep substitutes UX investment; governed surfaces (`SRC-INF-INT-050`) substitute with faster ratify cycles. |
| **Threat of new entrants** | Low-code analytics (Power BI etc. on other prongs) — UX prong must show **governed drill-down** moat (`SRC-INF-EXT-474`). |
| **Competition synthesis** | UX Infonomics on thin 10-row base: compete on **time-to-ratify** and **audit trail**, not chart count. Expand ledger **scheduled** via I96 closure artifacts, not fake OSINT padding. |

## Infonomics hook

**Economic levers:** operator minutes saved per ratify gate, reduced support escalations, confidence-adjusted schedule value (`SRC-INF-INT-187`), telemetry cost per dashboard widget (avoid `SRC-INF-EXT-487` anti-patterns).

**Holistika delta:** UX value is **Quality Fabric composed** (`SRC-INF-INT-050`) — monetize as enablement of governed decisions, not BI seat licenses.

**Govern options (ranked; no vault edit):**

1. **OPTION A (recommended)** — Define UX ROI metric `ratify_cycle_minutes_saved` on Research Center + ERP insight surfaces; link to BI consumer rows (BL-DATA) — measurable with existing UAT screenshots.
2. **OPTION B** — Extend UX_DISCIPLINE with Infonomics appendix citing thin prong + scheduled I96 evidence harvest — doctrine-first, slower quantification.
3. **OPTION C** — Adopt commercial product analytics (Mixpanel-class) — skeptic attribution limits (`SRC-INF-EXT-488`); weak for B2B operator ERP.
4. **OPTION D** — Defer UX valuation until Figma registry parity (BL-MKT) — **scheduled** post design-system closure; not dropped.
