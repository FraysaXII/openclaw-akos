# Self-Checkpoint — Pre-P2 (Column Promotion, Stage B)

| Key | Value |
|:---|:---|
| Initiative | I86 (initiative-cluster-execution-coordinator) |
| Phase | P2 entry — promote anchors from `notes` prefix to first-class `program_anchors` column |
| Author | Agent (Madeira / AKOS) |
| Date | 2026-05-17 |
| Posture | MANDATORY pause-point category (canonical-CSV gate + Supabase DDL); operator pre-authorised continuous execution under `decision_source: agent_inline_default_accepted_via_explicit_continue` (mirrors P1 posture) |
| Plan | `~/.cursor/plans/i86_program_anchor_robustness_3e15859c.plan.md` |

## 1. What I have read (Stage A landscape)

- [`akos/hlk_initiative_registry_csv.py`](../../../../../akos/hlk_initiative_registry_csv.py) — `INITIATIVE_REGISTRY_FIELDNAMES` tuple at L36-62 (24 columns). New `program_anchors` goes between `linked_topic_ids` (L56) and `notes` (L57). Per the comment header (L11-12), CSV columns must match this tuple exactly.
- [`scripts/validate_initiative_registry.py`](../../../../../scripts/validate_initiative_registry.py) — FK-resolution patterns at L150-156 (linked_topic_ids + manifests_processes). New `program_anchors` FK block mirrors L150-152 shape against `PROGRAM_REGISTRY.csv` `program_id` (path already loaded by `_load_csv_set` at L80-81 — must add a new `PROGRAM_CSV` constant + load).
- [`supabase/migrations/20260506120100_i59_initiative_registry_mirror.sql`](../../../../../supabase/migrations/20260506120100_i59_initiative_registry_mirror.sql) — reference shape (L10-37 column list, L44-53 indexes, L55-65 RLS).
- [`supabase/migrations/20260514193709_i71_p4_review_stamp.sql`](../../../../../supabase/migrations/20260514193709_i71_p4_review_stamp.sql) — I71 review-stamp `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` pattern (L54-58 process_list_mirror block); P2 mirrors this idempotent-additive shape for one column on one table.
- [`scripts/sync_compliance_mirrors_from_csv.py`](../../../../../scripts/sync_compliance_mirrors_from_csv.py) L668-692 — `_emit_initiative_registry_upserts` uses `INITIATIVE_REGISTRY_FIELDNAMES` directly; will auto-pick up `program_anchors` once the tuple is extended.
- [`docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/INITIATIVE_REGISTRY.csv`](../../../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/INITIATIVE_REGISTRY.csv) — current state: 68 rows; 24 carry `Program anchors:` prefix in `notes` (17 backfilled in P1 + 7 that already existed pre-I86 per the operator's manual edits). All 24 are eligible for one-shot migration.
- [`docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/dimensions/PROGRAM_REGISTRY.csv`](../../../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/dimensions/PROGRAM_REGISTRY.csv) — 13 active `PRJ-HOL-*-YYYY` program_ids; PK column `program_id`. FK target for the new `program_anchors` column.
- [`docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/CANONICAL_REGISTRY.csv`](../../../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/CANONICAL_REGISTRY.csv) row `canonical_id=initiative_registry` — `last_review: 2026-05-12`; updates to `2026-05-17` at P2 closure.
- [`docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/PRECEDENCE.md`](../../../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/PRECEDENCE.md) L49 (canonical CSV row) + L96 (mirror row) — both narratives extend to mention `program_anchors` column.

## 2. P2 deliverable plan (a-h from the plan body)

| # | Deliverable | File | Status entering checkpoint |
|:---|:---|:---|:---|
| a | Supabase migration | `supabase/migrations/20260517_i86_p2_program_anchors_column.sql` | not yet authored |
| b | Pydantic fieldnames | `akos/hlk_initiative_registry_csv.py` (add `program_anchors` after `linked_topic_ids`) | not yet authored |
| b' | CSV header + 68 rows | `docs/references/hlk/v3.0/...INITIATIVE_REGISTRY.csv` (insert column; populate via one-shot) | not yet authored |
| c | Validator FK block | `scripts/validate_initiative_registry.py` (add `program_anchors` FK to PROGRAM_REGISTRY.csv) | not yet authored |
| d | One-shot conversion | `scripts/_oneshot_anchors_notes_to_column.py` (--dry-run default) | not yet authored |
| e | Switch Stage-A validator default to column-read | `scripts/validate_initiative_program_anchors.py` (+ `--legacy-notes-parser`) | not yet authored |
| f | Mirror parity verification | `py scripts/verify.py compliance_mirror_emit` (Supabase apply deferred to operator) | not yet run |
| g | CANONICAL_REGISTRY.csv + PRECEDENCE.md updates | (paths above) | not yet authored |
| h | Pause record | `docs/wip/planning/86-.../reports/p2-pause-record-2026-05-17.md` | not yet authored |

## 3. Decisions applied autonomously (per operator pre-authorisation)

| Decision | Choice | Rationale |
|:---|:---|:---|
| **Column placement in CSV header** | After `linked_topic_ids`, before `notes` | Matches Pydantic tuple location (L56-57); semantic adjacency (both `linked_topic_ids` and `program_anchors` are semicolon-list FK columns); keeps `notes` as the trailing free-text field |
| **Supabase column type** | `TEXT` (semicolon-list literal, matching `linked_topic_ids` shape) | Consistent with existing semicolon-list columns; SQL-side `string_to_array(program_anchors, ';')` view at P3 produces array for joins |
| **Index** | Btree on `program_anchors` text (for `LIKE '%PRJ-HOL-XYZ%'` lookup) | Cheap; matches I59 P1.2 `linked_topic_ids` non-indexed posture relaxed for the rollup-join hot path at P3 |
| **One-shot script naming** | `scripts/_oneshot_anchors_notes_to_column.py` (underscore prefix) | Underscore prefix marks "remove after success" (matches I83 P3 `_oneshot_*.py` convention) |
| **Migration timestamp** | `20260517T<HHMMSS>_i86_p2_program_anchors_column.sql` | Follows I71 P4 review-stamp + I84 P3 substrate-registry timestamp convention |
| **Migration apply posture** | Author SQL + author one-shot + run one-shot in `--apply` mode against CSV; **defer Supabase remote apply** to operator (cannot apply DDL without operator credentials) | Per operator SQL gate (`akos-holistika-operations.mdc` §"Operator SQL gate"): I propose; operator applies. CSV side is canonical and can move ahead; mirror catches up on next `compliance_mirror_emit` run by operator |
| **Validator default cutover** | Stage A validator (`validate_initiative_program_anchors.py`) switches default to column-read; emits WARN if any row still carries `Program anchors:` prefix in `notes` post-cutover; `--legacy-notes-parser` flag preserves Stage A behavior for one cycle | Per D-IH-86-J Stage-B contract: column is authoritative; notes-prefix becomes deprecated drift signal |

## 4. Outstanding before P2 close

1. Author migration SQL + Pydantic tuple + validator FK block + one-shot + cutover.
2. Run one-shot `--dry-run` (audit), then `--apply` (mutate CSV).
3. Re-run `validate_hlk.py` end-to-end; confirm no regressions; new FK block green.
4. Run `py scripts/verify.py compliance_mirror_emit` to confirm CSV→SQL emit picks up the new column.
5. Author + ship `reports/p2-pause-record-2026-05-17.md` enumerating operator-side residual actions (Supabase apply + mirror reseed).
6. Update CANONICAL_REGISTRY + PRECEDENCE.
7. Update CHANGELOG.md + master-roadmap.md + files-modified.csv.
8. File mid-P2 + post-P2 self-checkpoints (per plan: SC count = 3 at P2).
9. Transition to P3.

## 5. Risk monitor

- **R-IH-86-7** (notes-prefix parse rots silently): mitigated when Stage B cutover lands — column becomes authoritative; legacy parser becomes opt-in via `--legacy-notes-parser` flag with single-cycle deprecation window.
- **R-IH-86-8** (`PRJ-HOL-*` ids leak to adviser-external surfaces): BBR drift gate already extended in P1; further hardening (adviser-surface scope) deferred to P3.
- **R-IH-86-9** (Stage A advisory-only validator): closes at P2 — column FK becomes mandatory in `validate_initiative_registry.py` (fail-loud against PROGRAM_REGISTRY).

## 6. References

- I86 plan body §P2 (lines 173-224 of the cursor plan file).
- D-IH-86-J (column promotion decision) — `decision-log.md` Round 2 §J.
- `akos-holistika-operations.mdc` §"Two-plane model" + §"Operator SQL gate".
- `akos-agent-checkpoint-discipline.mdc` §"Pause-point depth heuristic" (canonical-CSV gate → mandatory; agent self-checkpoint count 3 for ≥6-deliverable phases).
- `akos-governance-remediation.mdc` §"HLK compliance governance" — canonical-CSV gate discipline.
