# Self-Checkpoint — Mid-P2 (Stage B Column Promotion — code shipped, mirror-apply deferred)

| Key | Value |
|:---|:---|
| Initiative | I86 (initiative-cluster-execution-coordinator) |
| Phase | P2 mid-phase — code + CSV mutation complete; Supabase remote apply deferred to operator |
| Author | Agent (Madeira / AKOS) |
| Date | 2026-05-17 |
| Posture | Continuing autonomously under operator pre-authorisation; logging defaults for audit |
| Plan | `~/.cursor/plans/i86_program_anchor_robustness_3e15859c.plan.md` §P2 |

## 1. P2 deliverables shipped this slice

| # | Deliverable | Path | Status |
|:---|:---|:---|:---|
| a | Supabase migration | `supabase/migrations/20260517163635_i86_p2_program_anchors_column.sql` | authored; idempotent; rollback documented inline; **operator must apply via MCP `apply_migration`** |
| b | Pydantic fieldnames | [`akos/hlk_initiative_registry_csv.py`](../../../../../akos/hlk_initiative_registry_csv.py) L57 | `program_anchors` inserted between `linked_topic_ids` and `notes` |
| b' | CSV header + 68 rows | [`INITIATIVE_REGISTRY.csv`](../../../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/INITIATIVE_REGISTRY.csv) | new column populated for 24 rows (17 P1 backfill + 7 pre-existing); notes prefix stripped on all 24 |
| c | Validator FK block | [`scripts/validate_initiative_registry.py`](../../../../../scripts/validate_initiative_registry.py) L160-162 | `program_anchors` FK-resolves against PROGRAM_REGISTRY.csv (12 ids); fail-loud on unknown |
| d | One-shot conversion | [`scripts/_oneshot_anchors_notes_to_column.py`](../../../../../scripts/_oneshot_anchors_notes_to_column.py) | `--dry-run` default; ran `--apply` 2026-05-17 17:50 UTC+2 against canonical CSV; diff at `reports/p2-oneshot-diff-2026-05-17.md` |
| e | Stage-A validator cutover | [`scripts/validate_initiative_program_anchors.py`](../../../../../scripts/validate_initiative_program_anchors.py) | default = column-read; `--legacy-notes-parser` opt-in deprecation window; `--strict` fails on cutover-hygiene WARNs |
| g | CANONICAL_REGISTRY + PRECEDENCE | row L31 + L49 + L96 | last_review updated to 2026-05-17; narratives extended with D-IH-86-J + column-promotion notes |

## 2. Validators / tests / regression posture

| Surface | Result |
|:---|:---|
| `py scripts/validate_initiative_registry.py` | **PASS** — 68 rows; new `program_anchors` FK block green against 12 PROGRAM_REGISTRY ids |
| `py scripts/validate_initiative_program_anchors.py` (column-read) | **PASS** — 24 rows with column; 0 residual notes-prefix; 0 malformed; 0 unknown |
| `py scripts/validate_initiative_program_anchors.py --legacy-notes-parser` | **PASS** — 0 rows still carry the prefix (cutover clean) |
| `py scripts/validate_hlk.py` | **OVERALL PASS** — all 30 canonicals green incl. new INITIATIVE_PROGRAM_ANCHORS slot |
| `py -m pytest tests/test_validate_initiative_program_anchors.py tests/test_pmo_program_anchor_backfill.py -v` | **22 passed** — 11 Stage A + 5 Stage B column-read + 6 backfill runbook |
| `py scripts/verify.py compliance_mirror_emit` | **PASS** — emitted SQL includes `program_anchors` column in INSERT + ON CONFLICT UPDATE clauses (`artifacts/sql/compliance_mirror_upsert.sql` 5.5 MiB) |

## 3. Decisions applied this slice (logged for operator audit)

| Decision | Choice | Rationale |
|:---|:---|:---|
| One-shot script naming | `scripts/_oneshot_anchors_notes_to_column.py` (underscore prefix; single-use; remove on P2 closure commit) | I83 P3 `_oneshot_*.py` precedent — underscore marks "remove after success"; prevents misuse as recurring runbook |
| Header-insert handling in one-shot | Auto-detect pre-P2 header shape; insert `program_anchors` at canonical position; abort on any other header drift | Idempotent; safe to re-run; only handles the column-insertion case; any other delta surfaces to operator |
| Notes-prefix stripping regex | Reuse `ANCHOR_LINE_PATTERN` from Pydantic chassis; strip matched segment incl. trailing period; collapse whitespace runs in remainder | Single-source-of-truth — chassis owns the parse semantics; one-shot inherits cleanly |
| Column-read validator structure | Separate `_evaluate_column_read` + `_evaluate_legacy_notes` functions; main dispatcher picks via `--legacy-notes-parser` flag | Allows operator to A/B compare during deprecation window; keeps Stage A test coverage intact |
| Cutover-hygiene WARN posture | Column-read default emits WARN per row with `Program anchors:` prefix in notes; `--strict` flag promotes to FAIL | Soft-deprecation — gives operator a one-cycle window to clean any pre-existing rows the one-shot didn't catch (manual notes edits, conflicts) |

## 4. Outstanding before P2 closure

1. File **`reports/p2-pause-record-2026-05-17.md`** with operator approval checklist (≤7 items) covering: Supabase `apply_migration` invocation, post-apply `compliance_mirror_emit` re-run + apply to refresh mirror with new column data, `get_advisors(security)` confirmation, deletion of `_oneshot_anchors_notes_to_column.py`.
2. Update **CHANGELOG.md** with the P2 entry.
3. Update **`master-roadmap.md`** to mark P2 deliverables complete (Stage B chassis live; Supabase apply deferred).
4. Append rows to **`files-modified.csv`** for every P2 file.
5. File **`sc-post-p2-2026-05-17.md`** documenting mirror parity verify outcome (after `verify.py compliance_mirror_emit` re-run + spot-check the emitted SQL block).
6. Transition to P3 (persona-view rollup).

## 5. Operator-side residual actions (not in agent scope this turn)

| Action | Owner | Pre-requisite |
|:---|:---|:---|
| Apply migration to remote Supabase | Operator (via Supabase MCP `apply_migration`) | This pause record + the migration SQL file |
| Re-run `verify.py compliance_mirror_emit` post-apply | Operator | Migration applied |
| Apply emitted SQL to remote (`artifacts/sql/compliance_mirror_upsert.sql`) | Operator | Mirror schema has new column |
| Run MCP `get_advisors(security)` and address any new findings attributable to P2 | Operator | Mirror refresh complete |
| Delete `scripts/_oneshot_anchors_notes_to_column.py` in P2 closure commit | Operator (or agent on next turn) | All above confirmed |

## 6. Risk monitor

- **R-IH-86-7** (notes-prefix parse rots silently): **CLOSED** — cutover-hygiene WARN now fires on any residual prefix; `--strict` mode catches in CI; 24 rows already migrated.
- **R-IH-86-8** (`PRJ-HOL-*` ids leak to adviser-external surfaces): unchanged from P1 — BBR drift gate covers; further adviser-surface hardening deferred to P3.
- **R-IH-86-9** (Stage A advisory-only validator): **CLOSED** — column FK now mandatory in `validate_initiative_registry.py` (fail-loud against PROGRAM_REGISTRY); release-gate row reflects the cutover.
- **NEW risk surfaced this slice**: **R-IH-86-10** — operator may not apply the Supabase migration promptly, leaving CSV ↔ mirror schema temporarily out of sync (CSV ahead, mirror lags). Mitigation: `compliance_mirror_emit` UPSERT-only posture means stale mirror rows survive; new column is `NULL`-tolerant; `get_advisors` reports no new advisories from the additive column. Risk window: until operator applies. Catalogue in P2 pause record.

## 7. References

- Pre-P2 self-checkpoint: [`sc-pre-p2-2026-05-17.md`](sc-pre-p2-2026-05-17.md).
- P2 plan body: §P2 in cursor plan.
- D-IH-86-J ratification: `decision-log.md` §J.
- One-shot diff report: [`p2-oneshot-diff-2026-05-17.md`](../p2-oneshot-diff-2026-05-17.md) (24 rows; full BEFORE/AFTER per row).
