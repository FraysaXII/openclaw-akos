---
report_type: neo4j-cq-uat
parent_initiative: INIT-OPENCLAW_AKOS-95
lane: L4-HCAM-P2-Neo4j
authored: 2026-06-09
authored_by: Execution seat (Composer)
ratifying_decisions:
  - D-IH-95-C
  - D-IH-95-F
status: BLOCKED-AUTH
verdict: FAIL
---

# I95 Neo4j competency questions — live UAT (2026-06-09)

**Charter:** [`i95-neo4j-e2e-cutover-charter-2026-06-09.md`](i95-neo4j-e2e-cutover-charter-2026-06-09.md) phase **N4**.  
**Prior cutover:** N3 dual-emit **APPLIED** per [`i95-neo4j-cutover-execution-2026-06-09.md`](i95-neo4j-cutover-execution-2026-06-09.md).  
**This session:** CQ1–CQ5 **not executed** — Aura authentication failed.

---

## Semantic Council sign-off stub (D-IH-95-F)

| Gate | Status | Note |
|:---|:---:|:---|
| Council disposition recorded | **STUB** | N3 live sync already executed 2026-06-09; formal Council minutes not minted in this session |
| Operator intent for N4 | **REQUESTED** | Packet C continuation after mirror recovery |
| Rollback tag | **DEFERRED** | `neo4j-pre-unified-*` tag not verified this session |

**Council action required before N6 legacy retire:** ratify unified-edge consumption + CQ green on live Aura.

---

## Credential preflight (masked)

| Check | Result |
|:---|:---|
| `NEO4J_URI` | present (`neo4j+s://…`) |
| `NEO4J_USERNAME` | present (non-default Aura user) |
| `NEO4J_PASSWORD` | present (length verified; value not logged) |
| `NEO4J_TRUST=all` | rewrites `neo4j+s` → `neo4j+ssc` per AKOS graph sync |

**Connection attempt:** `neo4j.exceptions.AuthError` — unauthorized (invalid credentials or rotated password not yet in `~/.openclaw/.env`).

---

## Competency question execution matrix

| ID | Question (summary) | Status | Evidence |
|:---|:---|:---:|:---|
| CQ1 | Role → processes → capabilities | **NOT RUN** | Auth blocked |
| CQ2 | Engagement end-to-end + roles | **NOT RUN** | Auth blocked |
| CQ3 | Capability retirement impact | **NOT RUN** | Auth blocked |
| CQ4 | Process composition path | **NOT RUN** | Auth blocked |
| CQ5 | Area orphan check | **NOT RUN** | Auth blocked |

**Query specs:** `akos/hlk_graph_articulation.py` → `COMPETENCY_QUESTIONS` (unified-edge Cypher).

**Re-run command (after operator fixes Aura password in `~/.openclaw/.env`):**

```powershell
py scripts/sync_hlk_neo4j.py --dry-run --dual-emit
# then execute CQ1–5 Cypher via graph MCP or a one-shot runner against live Aura
```

---

## Disposition

| Item | Status |
|:---|:---|
| N4 CQ UAT | **FAIL / BLOCKED-AUTH** |
| N5 consumer flip | **DEFERRED** |
| N6 legacy retire | **DEFERRED** — dual-emit window remains active |

**Operator next step:** rotate or re-copy Aura credentials into `~/.openclaw/.env`, confirm with `py scripts/sync_hlk_neo4j.py --dry-run`, then re-run this UAT row.
