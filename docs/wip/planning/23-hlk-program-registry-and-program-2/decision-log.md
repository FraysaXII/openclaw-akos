# Initiative 23 — Decision Log

| ID | Date | Decision | Rationale | Status |
|:---|:-----|:---------|:----------|:-------|
| **D-IH-8** | 2026-04-29 | **Program registry SSOT** = new canonical CSV `docs/references/hlk/compliance/dimensions/PROGRAM_REGISTRY.csv` (separate from `process_list.csv`). `program_id` is the canonical PRJ-HOL-style identifier (path-stable, used in folder slugs). `process_item_id` is a separate FK column to `process_list.csv` `item_id` (when one exists). `program_code` (`^[A-Z]{3}$`, unique) is the short handle for path slugs and graph node properties. | Process list keeps the project-tree row; the new CSV holds program metadata and program-to-program edges. Same pattern as GOI/POI dimension (Initiative 21). PRJ-HOL-style ids are stable across `process_list.csv` reorganizations and human-readable. | **APPROVED** (Wave-2 plan operator confirmation) |
| **D-IH-9** | 2026-04-29 | **Program-to-program edges** = denormalized text in CSV/Postgres + typed relationships in Neo4j. Validator enforces cycle detection (parent + consumes), `program_code` uniqueness, FK to baseline + process_list, **STRICT forward-reference policy** (every referenced `program_id` must exist in the same file at validation time), with `--allow-forward-references=<csv-list>` flag for the **single commit** that introduces a new program. | CSV-friendly diffing + DAMA-pure Postgres mirror. Neo4j is the graph DDL surface for typed relationships. Strict forward-reference avoids silent broken edges; the escape hatch keeps onboarding ergonomic. | **APPROVED** |
| **D-IH-16** | 2026-04-29 | **KiRBe duality** = `PRJ-HOL-KIRBE-2026` represents BOTH a SaaS product (kirbe.* schema, Stripe billing per `metadata.hlk_billing_plane`, telemetry `kirbe.monitoring_logs`) AND a vault KM ingestion source (`v3.0/index.md`: "Ingest as a source"). Program-2 onboarding role roots are **evidence-based** (Tech, Data Governance, Data Architecture, Finance, People, PMO required; Compliance/Legal/Marketing on-demand) — not preemptive. | Initiative 22 `programs/<program_id>/README.md` precedent under role roots avoids preemptive folders that would clutter without supporting `process_list.csv` evidence. The 6 required roots map to existing process rows; the 3 on-demand roots have no KiRBe-specific rows yet. | **APPROVED** |
| **D-IH-18** | 2026-04-29 | **Neo4j as canonical graph DDL surface** = wave-2 graph DDL extends Initiative 07's `:Role` + `:Process` + `:Document` schema with `:Program` plus typed edges (`:CONSUMES`, `:PRODUCES_FOR`, `:PROGRAM_PARENT_OF`, `:PROGRAM_SUBSUMES`, `:OWNED_BY` for `(:Program → :Role)`). Postgres mirrors hold denormalized text columns (DAMA-pure mirrors); Neo4j projects into typed relationships. **Discipline:** CSVs remain SSOT per `PRECEDENCE.md`; Neo4j is rebuildable read index; allowlisted Cypher in `akos/hlk_neo4j.py` only; sync = full rebuild via existing `wipe=True` pattern; graceful SKIP when Neo4j unconfigured. **Edge naming disambiguated** (`:PROGRAM_PARENT_OF` vs existing `:PARENT_OF` for processes). | Multi-hop graph queries are the right shape for program dependencies; Postgres relational engine is the wrong tool for a graph. Initiative 07 invariants are preserved (CSV SSOT, allowlisted Cypher, full rebuild). | **APPROVED** |
| **D-IH-23-A** | 2026-04-29 | **Tier-3 cell defaults seeded by agent in `operator-answers-wave2.yaml`**. Per Wave-2 plan T2 tier definition ("agent proposes defaults; operator flips outliers"), the agent fills `lifecycle_status`, `risk_class`, `start_date`, `target_close_date`, `consumes_program_ids`, `notes` with sensible defaults for the 11 existing programs (PRJ-HOL-FOUNDING-2026 was already filled). Each defaulted cell is annotated `# agent default — operator review` so the diff is reviewable. | Unblocks Initiative 23 P1 without waiting for operator interview. Operator overrides any cell at any time by editing the YAML and re-running the scaffolder. | **APPROVED (defaults documented in YAML diff)** |

## Re-evaluation triggers

- **D-IH-9** Forward-reference policy: review when the registry exceeds 30 programs (cycle-detection cost grows quadratically in semicolon lists).
- **D-IH-18** Promote Neo4j from optional to mandatory in the agent ladder when operator UAT shows actual reliance on graph queries (template at Initiative 26 P0 reports/re-eval-trigger-graph-mcp-tooling-promotion.md).
- **D-IH-23-A** Operator review of agent-defaulted Tier-3 cells: target review by 2026-05-15 or before next program onboarding, whichever is sooner.
