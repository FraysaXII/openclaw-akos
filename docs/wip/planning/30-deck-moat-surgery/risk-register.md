# Initiative 30 — Risk register

| ID | Risk | Likelihood | Impact | Mitigation |
|:---|:-----|:-----------|:-------|:-----------|
| **R-30-1** | Naming MADEIRA externally before the brand is ready (the deck plants a product that doesn't have a website / waitlist / pricing page yet). | Medium | Medium | The deck frames MADEIRA as **direction**, not GA. The slide-12 bullet says "primer cliente externo de MADEIRA" with no commit window. The `TODO[OPERATOR-madeira-saas-window]` in `MADEIRA_PLATFORM.md` keeps the productization window founder-controlled. If the brand is not deck-ready, downgrade slide-04/05 wording to "agentes estandarizados" without the proper noun (single-line revert in YAML). |
| **R-30-2** | Live `validate_hlk` numbers drift (a new topic, a new role, a new process row); the moat slide quotes a stale number. | High | Low | New `tests/test_governance_moat_metrics.py` parses live `validate_hlk` output and compares to `GOVERNANCE_MOAT.md` quoted metrics; +0/-0 tolerance for topics + roles, ±5 % for processes. CI failure surfaces the drift at PR time. Operator updates the artifact in <5 min and re-runs the build pipeline. |
| **R-30-3** | Jargon-audit gate flags MADEIRA as forbidden internal jargon. | Medium | Low | The brand-name allow-list in `BRAND_JARGON_AUDIT.md` already includes `KiRBe`, `Holistika`, `holistikaresearch.com`. If MADEIRA flags, extend the allow-list with one entry. P5 catches this early. |
| **R-30-4** | Joint-equity channel is misread by an ENISA certifier as a "joint venture" structure that disqualifies the company from "Empresa Emergente" status. | Low | High | `CHANNEL_STRATEGY.md` Channel 6 wording explicitly frames this as "Holistica delivers a SaaS for the partner; the partner pays Holistica in equity + revenue share — Holistica retains intellectual property and the engineering team is Holistica employees, not jointly owned". The channel is a commercial pattern, not a corporate-structure change. Cite this distinction in the artifact. |
| **R-30-5** | Slide 06 restructure produces visual breakage (e.g., 5 cards become 4 because card-internal is a single combined card and the layout assumes 5-card grid). | Medium | Low | The HTML preview deck rebuilds deterministically from YAML; visual breakage surfaces at P5 build time. The Figma layout primitive in `figma-link.md` is documented as "5-card capability grid"; this initiative keeps 5 cards (card-kirbe / card-madeira / card-shopify / card-rushly / card-internal). 4-card or 6-card layouts are out of scope. |
| **R-30-6** | The added Channel 6 in `CHANNEL_STRATEGY.md` makes the artifact too long for a reviewer (it grows from ~200 lines to ~250 lines). | Low | Low | Channel 6 follows the existing 5-channel format exactly (Hypothesis / ICP-match / leading indicator / current evidence / CAC band / Decision criteria / Why this works). Length growth is proportional and structurally consistent. |
| **R-30-7** | Tests pass locally but fail in CI due to encoding / line-ending differences in the new MD artifacts. | Low | Low | Use UTF-8 throughout; let git's `*.md text=auto` handle line endings. Same pattern as I29 commits — no new exposure. |

## Risk-acceptance posture

R-30-1 (naming MADEIRA before brand-ready) is the only Medium-likelihood × Medium-impact risk. Decision D-IH-30-A (in `decision-log.md`) accepts it explicitly: the upside (deck reads as 2-product company) outweighs the downside (one bullet on slide 12 references a future product). The fallback (downgrade to "agentes estandarizados" without the proper noun) is a 30-second YAML revert if needed.
