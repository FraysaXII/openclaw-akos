# Initiative 30 — Decision log

| ID | Date | Decision | Rationale | Status |
|:---|:-----|:---------|:----------|:-------|
| **D-IH-30-A** | 2026-04-30 | **Name MADEIRA on the public deck.** | MADEIRA is already a first-class internal concept (`SOP-MADEIRA_HLK_ERP_SHOWCASE_004.md`, `SOP-MADEIRA_ENVOYTECH_SHOWCASE_002.md`, `Think Big/`, KIR program onboarding). Naming it externally as the agentic-platform brand establishes the second product line (alongside KiRBe) and converts the deck from "1 product + 4 services" into "2 products + 2 demand signals + internal validation". `BRAND_JARGON_AUDIT.md` allow-list already permits brand names (`KiRBe`, `Holistika`); MADEIRA falls in the same category. | **Approved** |
| **D-IH-30-B** | 2026-04-30 | **Joint-equity equity-band stays `TODO[OPERATOR-channel6-equity-band]`** in the artifact, not narrowed in this initiative. | Setting an equity band is a founder-and-counsel decision (must align with corporate structure post-incorporation, ENISA "joint-venture" ineligibility carve-out, and existing investor expectations). Documenting the channel structurally without committing to a band on the public deck is the right posture: shows demand pattern, defers numbers. | **Approved** |
| **D-IH-30-C** | 2026-04-30 | **Demote `card-web` + `card-erp` into a single `card-internal` "Validación interna" card on slide 06.** Promote `card-kirbe` and a new `card-madeira` to hero positions; keep `card-shopify` + `card-rushly` as demand-signal cards. | The existing 5 cards over-weight services: 2 internal (web, ERP), 2 partner-anchored services (shopify, rushly), 1 product (kirbe). Restructuring to 2 hero products + 2 demand signals + 1 internal-validation card mirrors how investors actually read a proof grid. The internal-validation card preserves the "we eat our own cooking" message in 1 slot instead of 2. | **Approved** |
| **D-IH-30-D** | 2026-04-30 | **Replace moat pillar 1 ("Internal-first") with quantified governance.** New pillar 1 quotes `17 temas gobernados / 1.093 procesos / 65 roles / validadores + sondas de drift` directly from `GOVERNANCE_MOAT.md`. Internal-first message folds into the new `card-internal` on slide 06 to avoid duplication. | Pillar 1 was an unquantified claim ("we operate with our own products before selling them"). Investors count costly signals — replacing prose with measured numbers turns the moat into something they can audit. The internal-first message is preserved structurally; only its slot moves. | **Approved** |
| **D-IH-30-E** | 2026-04-30 | **Figma backport DEFERRED to a later session.** YAML/Markdown SSOT carries the I30 edits; the Figma file at `https://www.figma.com/design/yiPav7BLxUulNFrrsoKJqW` stays on its current (I29 P1) frame copy until the founder picks a working session. | Per the I29 P1 drift-handling rule: YAML wins for content, Figma wins for layout, edits backport to YAML before close. This initiative is content-led; backport is layout-led. Splitting them keeps each session focused and prevents Figma fatigue mid-PR. The deferral is logged in `[figma-link.md](../../references/hlk/v3.0/_assets/advops/PRJ-HOL-FOUNDING-2026/enisa_company_dossier/figma-link.md)` for the operator. | **Deferred (operator-scheduled)** |
| **D-IH-30-F** | 2026-04-30 | **`GOVERNANCE_MOAT.md` quoted metrics are validated against live `validate_hlk` output via a new test (`test_governance_moat_metrics.py`)**, with a tolerance band of +0/-0 for topics + roles, and ±5 % for processes. | Hard-coded metrics rot. The validator output is the canonical source; the test enforces parity at PR-time so any future drift (added topic / new role / new process row) surfaces as a CI failure rather than a stale deck slide. The wider tolerance on processes accommodates daily ops growth without nuisance failures. | **Approved** |
