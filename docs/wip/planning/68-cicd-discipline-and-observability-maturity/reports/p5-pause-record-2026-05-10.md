---
language: en
status: active
initiative: 68-cicd-discipline-and-observability-maturity
report_kind: pause-record
phase: P5
last_review: 2026-05-10
pause_point: "#3 — canonical CSV gate"
pause_class: explicit_operator (mandatory per [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) §"HLK compliance governance" — canonical CSV gate)
---

# I68 P5 — PAUSE POINT #3 (canonical CSV gate) — AKOS-side complete; CSV bumps + bless extension await operator approval

**Phase scope.** Mint the AKOS-side P5 deliverables: `SOP-CICD_BASELINE_001.md` at `status: review` (promoted to `active`/`v1.0.0` in P8 closure), the canonical GitHub Actions workflow template, the canonical Render YAML stub, the `akos.cicd_baseline` Pydantic models, the `validate_cicd_baseline.py` validator (forward-compatible: tolerates missing CSV columns until the gate ships them), the test suite, the release-gate wiring, the `cicd_baseline_check` profile extension, and the `akos-docs-config-sync.mdc` row mapping the SOP to its downstream surfaces. **Explicitly gated** at PAUSE POINT #3 per [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) §"HLK compliance governance — Canonical CSV gates": the canonical CSV bumps to [`REPOSITORY_REGISTRY.csv`](../../../references/hlk/compliance/REPOSITORY_REGISTRY.csv) (3 new columns: `ci_baseline_version`, `build_time_target_seconds`, `ci_baseline_optouts`) + 3 new `process_list.csv` rows (`env_tech_dtp_*` for `cicd_baseline_maintenance` / `observability_dashboard_review` / `visual_regression_triage`) + `bless_external_repo.py --with ci-baseline` flag extension are NOT shipped from this commit and require explicit operator approval at this pause point. Round-2 plan reference: `~/.cursor/plans/i68_cicd_activation_roadmap_592a78e2.plan.md` §P5.

## Mechanical evidence (what shipped from AKOS workspace)

| Gate | Result |
|:---|:---|
| Canonical SOP at [`docs/references/hlk/v3.0/Admin/O5-1/Tech/System Owner/SOP-CICD_BASELINE_001.md`](../../../references/hlk/v3.0/Admin/O5-1/Tech/System Owner/SOP-CICD_BASELINE_001.md) at `status: review` + `version: v0.9.0` (promoted to `active`+`v1.0.0` at I68 P8 closure); §1 purpose (cites I66 P5 incident as motivation); §2 scope; §3 per-class baseline check matrix (4 classes × 11 checks; per-check opt-out string identifiers); §4 workflow templates (canonical GHA + Render YAML stub references); §5 per-platform preview-protection bypass (Vercel `VERCEL_AUTOMATION_BYPASS_SECRET` + Render preview-discipline equivalent); §6 operator-driven adoption flow (`bless_external_repo.py --with ci-baseline`); §7 per-repo opt-out matrix (D-IH-68-J) with worked examples for `kirbe-platform`/`boilerplate`/`hlk-erp`; §8 verification (3 validators); §9 SOP change-control via SOP-META; §10 cross-references | PASS |
| Canonical GitHub Actions workflow template at [`docs/references/hlk/v3.0/Envoy Tech Lab/Repositories/_templates/github-workflows/ci-baseline.yml.tmpl`](../../../references/hlk/v3.0/Envoy Tech Lab/Repositories/_templates/github-workflows/ci-baseline.yml.tmpl) — header documents class enforcement + opt-out semantics + required repo secrets; jobs cover lint + typecheck + unit-test + playwright-smoke + visual-regression (PR-from-fork hardening per R-IH-68-12) + brand-jargon + sentry-release-format + build-time-target (with placeholder for `<BUILD_TIME_TARGET_SECONDS_PLACEHOLDER>` substituted at bless-time) | PASS |
| Canonical Render YAML stub at [`docs/references/hlk/v3.0/Envoy Tech Lab/Repositories/_templates/render/render-baseline.yaml.tmpl`](../../../references/hlk/v3.0/Envoy Tech Lab/Repositories/_templates/render/render-baseline.yaml.tmpl) — documents 5 patterns: build-cache (BuildKit + buildFilter), healthcheck (`/health` returns release-format JSON), cold-start (numInstances ≥ 1; no scale-to-zero), auto-deploy filter (main → production; preview-* → preview), Sentry release-tag env var injection | PASS |
| Pydantic models at [`akos/cicd_baseline.py`](../../../../akos/cicd_baseline.py) — `RepoClass` Literal `platform` / `reference` / `internal` / `client-delivery`; `KNOWN_CHECK_OPTOUTS` frozenset (11 check names + 6 inherited from I68 P2 Playwright per-viewport opt-outs); `PER_CLASS_REQUIRED_CHECKS` mapping per SOP §3 matrix; `PER_CLASS_DEFAULT_BUILD_TIME_TARGET_SECONDS` per D-IH-68-E (platform 300s, reference 120s, internal 60s, client-delivery 300s); `KNOWN_SOP_VERSIONS` (`v0.9.0` + `v1.0.0`); `CICDBaselineRow` with `ci_baseline_version` / `build_time_target_seconds ge=10 le=3600` / `ci_baseline_optouts list[str]` field validators; `required_checks()` + `missing_checks()` helpers; `from_registry_row()` factory tolerates missing columns | PASS |
| Validator at [`scripts/validate_cicd_baseline.py`](../../../../scripts/validate_cicd_baseline.py) — canonical SOP frontmatter `status:` + `version:` regex extraction; canonical workflow template + Render YAML stub presence checks; consumer-row scan opt-in via `AKOS_CICD_BASELINE_SCAN_CONSUMERS=1` (forward-compatible: missing columns → defaulted models, not failures); uses `akos.log.setup_logging` + `akos.io.REPO_ROOT` + `akos.cicd_baseline` per [`CONTRIBUTING.md`](../../../../CONTRIBUTING.md) §"Python Code Standards" | PASS |
| Test suite at [`tests/test_validate_cicd_baseline.py`](../../../../tests/test_validate_cicd_baseline.py) — 26 tests, all PASS in 0.47s. Covers: 13 `CICDBaselineRow` Pydantic tests (canonical platform + minimal internal + invalid slug/class/version/opt-out + known opt-out + per-viewport opt-out inherited from P2 + build-time bound rejects + per-class required-checks + opt-out-subtracts + missing-checks-diff); 5 `from_registry_row` tests (full-row + missing-columns-default + malformed-JSON-empty + legacy-semicolon + non-numeric-build-time); 4 constants smoke-checks; 4 validator integration tests (canonical SOP/templates present + default-soft + explicit-strict). Module-level `pytestmark = pytest.mark.cicd` | PASS (26 passed, 0 failed) |
| [`scripts/release-gate.py`](../../../../scripts/release-gate.py) wires `run_cicd_baseline_validation()` after `run_sentry_release_format_validation()` per the I68-cluster ordering convention | PASS |
| [`config/verification-profiles.json`](../../../../config/verification-profiles.json) `cicd_baseline_check` profile extended with `validate_cicd_baseline` step (now wraps all 3 I68 validators: P2 Playwright + P4 Sentry + P5 CICD baseline) | PASS |
| [`.cursor/rules/akos-docs-config-sync.mdc`](../../../../.cursor/rules/akos-docs-config-sync.mdc) gains a new doc-sync row mapping `SOP-CICD_BASELINE_001.md` to ALL downstream surfaces (Pydantic constants in `akos/cicd_baseline.py`; validator + tests; both workflow templates; `bless_external_repo.py` extension at PAUSE POINT #3; `check_external_repo_ci_posture.py` extension; `REPOSITORY_REGISTRY.csv` 3 new columns at PAUSE POINT #3; `process_list.csv` 3 new `env_tech_dtp_*` rows at PAUSE POINT #3; per-consumer-repo `.github/workflows/ci-baseline.yml` mirrors; per-consumer-repo `BASELINE_REALITY.md` justifications for opt-outs; `docs/USER_GUIDE.md` + `docs/ARCHITECTURE.md` "CICD Baseline" section; `akos-deploy-health.mdc`) | PASS |
| All P5 AKOS-side files lint-clean per ReadLints | PASS |
| `py scripts/validate_cicd_baseline.py` exits 0 (canonical SOP + templates OK; consumer scan skipped per default soft mode) | PASS |
| `py scripts/test.py cicd` runs 87 tests (27 Playwright + 34 Sentry + 26 CICD baseline), all PASS in 35.64s | PASS |

## Mechanical evidence (what is GATED at PAUSE POINT #3 and NOT shipped today)

| Gate | Status |
|:---|:---|
| `REPOSITORY_REGISTRY.csv` schema bump (3 new columns: `ci_baseline_version` / `build_time_target_seconds` / `ci_baseline_optouts`) | **AWAITING OPERATOR APPROVAL at PAUSE POINT #3** — canonical CSV gate per [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) §"HLK compliance governance — Canonical CSV gates" |
| `process_list.csv` 3 new `env_tech_dtp_NNN` rows (`cicd_baseline_maintenance` System Owner quarterly + `observability_dashboard_review` System Owner monthly + `visual_regression_triage` System Owner per-PR-as-needed) | **AWAITING OPERATOR APPROVAL at PAUSE POINT #3** — bundled with the schema bump per SOP-META rule (CSV before SOP); operator runs `py scripts/validate_hlk.py` post-approval to verify the rows |
| `scripts/bless_external_repo.py --with ci-baseline` flag extension (additive to existing `--with` flags from I63 bless infrastructure) | **AWAITING OPERATOR APPROVAL at PAUSE POINT #3** — depends on the `ci_baseline_version` CSV column being present + the SOP frontmatter `version` being readable; ships in the same commit as the CSV bumps |
| `scripts/check_external_repo_ci_posture.py` extension (cross-check `.github/workflows/ci-baseline.yml` sha256 per consumer repo against the recorded `REPOSITORY_REGISTRY.csv` value) | **AWAITING OPERATOR APPROVAL at PAUSE POINT #3** — ships in the same commit as the CSV bumps |
| Per-repo PR rollout (boilerplate canary → hlk-erp operator-internal → kirbe-platform production-customer-facing per R-IH-68-6 sequential rollout) | **DEFERRED — BLOCKED FROM AKOS WORKSPACE** per [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc): cross-repo PRs are operator follow-up after PAUSE POINT #3 lands the CSV bumps + bless flag |

## Documentary evidence

- **D-IH-68-D (per-class CI baseline)** — operationalised in `SOP-CICD_BASELINE_001.md` §3 + Pydantic `PER_CLASS_REQUIRED_CHECKS`. Closure pending P5(i) sibling-repo PR rollout post-PAUSE POINT #3.
- **D-IH-68-E (build-time targets)** — operationalised in `SOP-CICD_BASELINE_001.md` §4.1 GHA workflow `build-time-target` job + Pydantic `PER_CLASS_DEFAULT_BUILD_TIME_TARGET_SECONDS`. Closure pending P6 measurements + PAUSE POINT #3 CSV column.
- **D-IH-68-J (per-repo opt-out matrix)** — operationalised in `SOP-CICD_BASELINE_001.md` §7 + Pydantic `KNOWN_CHECK_OPTOUTS` + validator's `from_registry_row` opt-out parsing. Closure pending PAUSE POINT #3 `ci_baseline_optouts` JSON-array column landing on canonical CSV.
- **R-IH-68-6 (per-repo PR breaking)** — directly mitigated in §6 operator adoption flow (per-repo independent validation) + the sequential rollout order (boilerplate canary → hlk-erp operator-internal → kirbe-platform production-customer-facing).
- **R-IH-68-12 (Argos GitHub App PR-from-fork)** — directly mitigated in the GHA workflow template `visual-regression` job conditional (`pull_request_target` + explicit fork refusal unless `vetted-by-owner` label).
- **Cross-canon links the AKOS-side P5 stack honours:** [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) §"HLK compliance governance" (canonical CSV gate explicit; SOP-META order CSV-before-SOP); [`.cursor/rules/akos-deploy-health.mdc`](../../../../.cursor/rules/akos-deploy-health.mdc) §"Step 1" + §"Step 4" (per-class baseline operationalises the per-repo discipline at fleet scale); [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc) (template + SOP are AKOS SSOT; sibling-repo workflows mirror); [`CONTRIBUTING.md`](../../../../CONTRIBUTING.md) §"Python Code Standards" (Pydantic + type hints + structured logging + cross-platform pathlib + valid/invalid test pairs).

## Operator approval checklist for PAUSE POINT #3

The operator MUST approve every item below before the canonical CSV bumps + `bless_external_repo.py --with ci-baseline` extension + `check_external_repo_ci_posture.py` extension can ship. Each item is a separate decision; partial approval is allowed (e.g. approve schema bump but defer process_list rows to a follow-up commit).

1. **Approve `REPOSITORY_REGISTRY.csv` schema bump.** Add 3 new columns: `ci_baseline_version` (string; FK to SOP-CICD_BASELINE_001 frontmatter `version`; allowed values `v0.9.0` + `v1.0.0` at land time); `build_time_target_seconds` (integer; per-repo target; `>= 10 && <= 3600`; default per repo class); `ci_baseline_optouts` (JSON-array string; allowed values per [`akos/cicd_baseline.py`](../../../../akos/cicd_baseline.py) `KNOWN_CHECK_OPTOUTS` frozenset). The Pydantic model + validator are forward-compatible — until columns land, default-soft mode passes; once columns land, default-strict can be enabled via `AKOS_CICD_BASELINE_SCAN_CONSUMERS=1`. **Operator decision:** ☐ APPROVE / ☐ DEFER (note reason in next pause record).

2. **Approve 3 new `process_list.csv` rows.** `env_tech_dtp_NNN cicd_baseline_maintenance` (System Owner; quarterly; reviews + bumps SOP-CICD_BASELINE_001 version + per-repo CI workflow drift). `env_tech_dtp_NNN observability_dashboard_review` (System Owner; monthly; reviews the Sentry dashboard for cross-repo deploy-health regressions). `env_tech_dtp_NNN visual_regression_triage` (System Owner; per-PR-as-needed; triages Argos visual-regression diffs that the operator deems false positives or threshold-tunes). All 3 rows attach to `role_owner = system-owner` from `baseline_organisation.csv`. **Operator decision:** ☐ APPROVE / ☐ DEFER.

3. **Approve `scripts/bless_external_repo.py --with ci-baseline` extension.** Additive to existing `--with` flags. Stamps the canonical workflow template into a consumer repo's `.github/workflows/ci-baseline.yml` + (for Render repos) the Render YAML stub into `render.yaml`; sets `ci_baseline_version` to the SOP frontmatter `version` + `build_time_target_seconds` to the per-class default + `ci_baseline_optouts` to `[]`; sha256-stamps the resulting workflow file in `REPOSITORY_REGISTRY.csv`. Idempotent per the existing bless pattern. **Operator decision:** ☐ APPROVE / ☐ DEFER.

4. **Approve `scripts/check_external_repo_ci_posture.py` extension.** Cross-checks each `class=platform` repo's actual `.github/workflows/ci-baseline.yml` sha256 against the value recorded in `REPOSITORY_REGISTRY.csv` at bless time; emits a per-repo drift report with severity ranking; runs nightly in CI per existing I63 pattern (no incremental CI cost). **Operator decision:** ☐ APPROVE / ☐ DEFER.

5. **Confirm sequential rollout order.** Per R-IH-68-6 mitigation: boilerplate (canary; smallest blast radius; class=reference) → hlk-erp (operator-internal; class=platform; ~5 operator users) → kirbe-platform (production-customer-facing; class=platform; requires KirBe team review window). Each PR validated independently; failure of one does not auto-promote the next. **Operator decision:** ☐ APPROVE / ☐ AMEND (specify alternate order with rationale).

## Pre-P6 + P7 + P8 self-checkpoint (next concrete actions)

1. **P6 is BLOCKED** from AKOS workspace (build-time pre/post measurements require Vercel + Render API calls per repo; sibling-repo PRs to apply optimisations). AKOS-side: P6 pre-baseline measurement script + per-repo measurement CSV format spec can ship from AKOS workspace as preparation. Skip until operator approves PAUSE POINT #3 + P5(i) PR rollout begins.
2. **P7 has AKOS-side deliverables** that ship from this workspace UP TO PAUSE POINT #4 (page-spec gate): `reports/p7-page-spec-impeccable-2026-05-NN.md` mirroring I64 v2 page-spec discipline + Impeccable laws + user journeys + IA verdict-band-pattern + explicit anti-patterns. The TSX code in `hlk-erp/app/operator/infra-monitor/` is BLOCKED from AKOS workspace (sibling repo). Mark `p7-inframonitor-v0-with-infrahealth-module` as `in_progress` after the P5 commit lands.
3. **P8 has AKOS-side deliverables**: extending [`.cursor/rules/akos-deploy-health.mdc`](../../../../.cursor/rules/akos-deploy-health.mdc) §"Common build failures" with new Failure-N entries surfaced during the P2-P5 AKOS-side work; scaffolding the I69 candidate at `docs/wip/planning/_candidates/i69-inframonitor-saas-product.md`. Closure UAT requires P0-P7 fully complete and depends on sibling-repo work.

## Cursor-rules adherence summary

- [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) — canonical CSV gate at PAUSE POINT #3 explicitly honoured (CSV bumps + process_list rows + bless extension are EXPLICITLY HELD OUT of this commit and require operator approval); one-commit-per-phase honoured (P5 commit ships only the AKOS-side ungated work; the post-PAUSE-POINT-#3 commit will carry the CSV bumps + extensions); reuse-only (extends `akos/`, `scripts/release-gate.py`, `config/verification-profiles.json`, `tests/`, `pyproject.toml` markers — no parallel framework introduced); HLK compliance asset classification followed; SOP-META order respected (CSV-bump approval gates the SOP promotion to `active` in P8).
- [`.cursor/rules/akos-planning-traceability.mdc`](../../../../.cursor/rules/akos-planning-traceability.mdc) — plan-quality bar honoured.
- [`.cursor/rules/akos-agent-checkpoint-discipline.mdc`](../../../../.cursor/rules/akos-agent-checkpoint-discipline.mdc) — explicit operator pause-point #3 record filed; pause class is `explicit_operator (mandatory canonical CSV gate)` per the rule's classification matrix.
- [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc) — AKOS SOP + Pydantic + workflow templates are SSOT; sibling-repo workflows mirror; agent does NOT invent sibling-repo PRs from this workspace.
- [`.cursor/rules/akos-docs-config-sync.mdc`](../../../../.cursor/rules/akos-docs-config-sync.mdc) — new doc-sync row added for the canonical SOP → all downstream surfaces.
- [`CONTRIBUTING.md`](../../../../CONTRIBUTING.md) §"Python Code Standards" + §"Testing Standards" — Pydantic models with field validators + type hints + structured logging via `akos.log` + cross-platform pathlib via `akos.io.REPO_ROOT` + valid/invalid test pairs all honoured.
