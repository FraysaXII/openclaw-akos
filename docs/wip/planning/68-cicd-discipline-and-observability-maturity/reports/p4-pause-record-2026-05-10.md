---
language: en
status: active
initiative: 68-cicd-discipline-and-observability-maturity
report_kind: pause-record
phase: P4
last_review: 2026-05-10
pause_point: "self-checkpoint (mid-phase) — sibling-repo Sentry config carry-over blocker"
pause_class: agent_self_checkpoint (per [`.cursor/rules/akos-agent-checkpoint-discipline.mdc`](../../../../.cursor/rules/akos-agent-checkpoint-discipline.mdc) §"Agent self-checkpoints"; not an explicit operator pause-point — P4 has no operator pause-point per Round-2 plan §P4)
---

# I68 P4 — Sentry deploy-health telemetry (AKOS-side complete; sibling-repo Sentry init carry-over blocked)

**Phase scope.** Mint the AKOS-side Sentry release-format Pydantic models, the consumer-repo scanner validator, the canonical `SENTRY_DASHBOARD_HOLISTIKA.md` operator runbook, the test suite, the release-gate wiring, the `cicd_baseline_check` profile extension, and the `akos-docs-config-sync.mdc` doc-sync row. Sibling-repo PRs (boilerplate + hlk-erp `sentry.*.config.ts` + `next.config.mjs withSentryConfig` updates; kirbe-platform `sentry_sdk.init(...)` update) are deferred to operator follow-up because cross-repo PR creation is not safe from the AKOS workspace per [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc). Round-2 plan reference: `~/.cursor/plans/i68_cicd_activation_roadmap_592a78e2.plan.md` §P4.

## Mechanical evidence

| Gate | Result |
|:---|:---|
| Pydantic models at [`akos/sentry_release.py`](../../../../akos/sentry_release.py) — `SentryReleaseFormatRule` (rejects every non-canonical `release_template`); `SentryReleaseValue` (with `repo_slug` + `sha_short` field validators); `parse_release_value()` helper; `RELEASE_TEMPLATE_CANONICAL = '{repo_slug}@{sha_short}'`; `REPO_SLUG_RE` lowercase kebab-case `[a-z0-9][a-z0-9-]+[a-z0-9]`; `SHA_SHORT_RE` 7-12 char lowercase hex; `RELEASE_VALUE_RE` for parsing concrete values | PASS |
| Validator at [`scripts/validate_sentry_release_format.py`](../../../../scripts/validate_sentry_release_format.py) — canonical-doc strict validation always runs (presence + carries `RELEASE_TEMPLATE_CANONICAL` example + Pydantic rule still accepts the canonical template); consumer-repo scan opt-in via `AKOS_SENTRY_RELEASE_SCAN_CONSUMERS=1` env var; scans Next.js (`sentry.client.config.ts` + `sentry.server.config.ts` + `sentry.edge.config.ts` + `next.config.mjs withSentryConfig`) and Python (`main.py`/`app.py`/`sentry_init.py` + `sentry_sdk.init(...)` callsites); accepts known templating placeholders (`${process.env.VERCEL_GIT_COMMIT_SHA}`, `${RENDER_GIT_COMMIT}`, `os.environ['...']`, `{{...}}`); uses `akos.log.setup_logging` + `akos.io.REPO_ROOT` + `akos.sentry_release` per [`CONTRIBUTING.md`](../../../../CONTRIBUTING.md) §"Python Code Standards" | PASS |
| Canonical doc at [`docs/references/hlk/v3.0/Envoy Tech Lab/Repositories/SENTRY_DASHBOARD_HOLISTIKA.md`](../../../references/hlk/v3.0/Envoy Tech Lab/Repositories/SENTRY_DASHBOARD_HOLISTIKA.md) — `status: review` (promoted to `active` in P8); §1 why-this-doc-not-Sentry-API rationale (3 reasons cross-referenced to D-IH-68-G); §2 canonical release-format contract (D-IH-68-I) with worked examples for all 3 platform repos; §3 per-platform Sentry init code (Next.js Vercel + Python FastAPI Render); §4 dashboard YAML configuration (6 widgets: deploy-success-rate-7d/30d, build-time-trend-30d, source-map-upload-health-7d, release-volume-sparkline-30d, error-rate-by-release-7d) operator pastes into Sentry UI editor; §5 deliberate omissions; §6 migration plan when D-IH-68-G re-opens; §7 cross-references | PASS |
| Test suite at [`tests/test_validate_sentry_release_format.py`](../../../../tests/test_validate_sentry_release_format.py) — 34 tests, all PASS in 49.75s. Covers: 4 `SentryReleaseFormatRule` tests (canonical + 3 drift forms); 10 `SentryReleaseValue.parse` tests (5 canonical + 8 drift); 2 direct-construct invalid-field tests; canonical-doc presence; default-soft + explicit-strict mode; Next.js + Python release-string regex extraction; `_validate_release_value` cases (canonical pass, missing-`@` flag, wrong-repo-prefix flag, dash-separator flag, Vercel template placeholder accept, Render template placeholder accept, garbage SHA flag); 3 constants smoke-checks. Module-level `pytestmark = pytest.mark.cicd` so the suite is selectable via `py scripts/test.py cicd` | PASS (34 passed, 0 failed) |
| [`scripts/release-gate.py`](../../../../scripts/release-gate.py) wires `run_sentry_release_format_validation()` after `run_playwright_baseline_validation()` (which precedes `run_brand_jargon_validation()`) — both new validators ship as adjacent steps following the I68-cicd-baseline cluster pattern; results row carries the env-var-aware label distinguishing soft (canonical-doc-only) vs strict (consumer-scan) modes | PASS |
| [`config/verification-profiles.json`](../../../../config/verification-profiles.json) `cicd_baseline_check` profile extended with `validate_sentry_release_format` step (P5 will append `validate_cicd_baseline.py`); description updated to enumerate the env-var-gated consumer-scan opt-ins | PASS |
| [`.cursor/rules/akos-docs-config-sync.mdc`](../../../../.cursor/rules/akos-docs-config-sync.mdc) gains a new doc-sync row mapping `SENTRY_DASHBOARD_HOLISTIKA.md` to ALL downstream surfaces (Pydantic constants in `akos/sentry_release.py`; validator + tests; per-consumer-repo Sentry init sites for both Next.js + Python; `docs/ARCHITECTURE.md` Observability section + `docs/USER_GUIDE.md` operator runbook section + I68 P5 SOP-CICD_BASELINE_001 + `akos-deploy-health.mdc` §"Step 1") | PASS |
| All P4 AKOS-side files lint-clean per ReadLints | PASS |
| `py scripts/validate_sentry_release_format.py` exits 0 (canonical doc OK; consumer scan skipped per default soft mode) | PASS |
| `py scripts/test.py cicd` runs 61 tests (27 Playwright + 34 Sentry), all PASS in 35.21s | PASS |
| Sibling-repo Sentry init carry-overs (P4(f)): boilerplate + hlk-erp `sentry.*.config.ts` + `next.config.mjs withSentryConfig`; kirbe-platform `sentry_sdk.init(...)` updates | DEFERRED — BLOCKED FROM AKOS WORKSPACE per [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc): cross-repo PRs require sibling-repo working tree access |
| Sentry UI dashboard pasted by operator + 24h verification of all 3 platform repos showing non-zero release events | DEFERRED — operator-run after sibling-repo Sentry init carry-overs land |

## Documentary evidence

- **D-IH-68-C (Sentry sample-rate strategy)** — operationalised but NOT yet closed in [`decision-log.md`](../decision-log.md). Closure is gated on the sibling-repo carry-overs in P4(f) — only after a real sibling-repo PR proves production sample 1.0 + skip-on-preview can D-IH-68-C move from `proposed` → `decided`. AKOS-side prerequisite (canonical doc `SENTRY_DASHBOARD_HOLISTIKA.md` §3 spelling out the per-repo standard) is now PASS.
- **D-IH-68-G interim posture (stay vendor-managed)** — operationalised in `SENTRY_DASHBOARD_HOLISTIKA.md` §1 + §6 migration plan. The AKOS-side surface is now ready; closure stays at `interim` until cost ceiling triggers a re-evaluation.
- **D-IH-68-I (cross-repo release SHA strategy)** — operationalised: the Pydantic rule + validator + canonical-doc §2 + §3 per-platform examples + per-language regex extractor enforce the `<repo_slug>@<sha_short>` format end-to-end. Closure pending P4(f) sibling-repo carry-overs.
- **R-IH-68-2 (Sentry vendor lock-in cost)** — directly mitigated by the markdown-as-config approach: §6 of the canonical doc documents the Sentry → Datadog → self-hosted Grafana migration path; Pydantic models + validator are vendor-agnostic and extract release-string contracts from any vendor's init code.
- **R-IH-68-12 (Argos GitHub App PR-from-fork)** — not relevant to P4 (Argos is a P3 concern).
- **Cross-canon links the AKOS-side P4 stack honours:** [`.cursor/rules/akos-deploy-health.mdc`](../../../../.cursor/rules/akos-deploy-health.mdc) §"Step 1 — Deploy status check" (this dashboard operationalises that step at fleet scale); [`CONTRIBUTING.md`](../../../../CONTRIBUTING.md) §"Python Code Standards" (Pydantic + type hints + structured logging + cross-platform pathlib + valid/invalid test pairs); [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) §"Reuse/extension only" (extends existing release-gate + verification-profiles + akos/ — no parallel framework introduced).

## Why the sibling-repo Sentry init carry-overs are BLOCKED FROM AKOS workspace

Same reasoning as P2: the AKOS workspace at `c:\Users\Shadow\cd_shadow\openclaw-akos\` is the AKOS HLK SSOT root, not a sibling-repo working tree. Modifying `boilerplate/sentry.*.config.ts` requires the `boilerplate` working tree at `c:\Users\Shadow\cd_shadow\boilerplate\` (which the AKOS agent does not have direct access to from this session) plus `gh pr create` against `FraysaXII/boilerplate`. Inventing those PRs from this session would violate [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc) (AKOS template is SSOT; sibling repos mirror — never the other way around) and [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) (forbids creating files outside the AKOS root without explicit operator approval).

The AKOS-side prerequisite is now in place: every sibling-repo PR can be authored against the canonical Pydantic-validated format (`{repo_slug}@{sha_short}`) + the `SENTRY_DASHBOARD_HOLISTIKA.md` §3 per-platform code template + the validator's regex spec + tests.

## Operator follow-up checklist (not a pause point — informational)

The 4 explicit operator pause-points are P0 (already approved per the standing operator instruction "do not stop until all to-dos are done"), P3 (PR-comment UX review), P5 (canonical CSV gate), P7 (page-spec gate). This P4 self-checkpoint enumerates the operator-driven sibling-repo work that needs to happen between P4 AKOS-side completion and the P5 pause point:

1. Open the `boilerplate` working tree. Confirm `sentry.client.config.ts` and `sentry.server.config.ts` already match the §3 template from [`SENTRY_DASHBOARD_HOLISTIKA.md`](../../../references/hlk/v3.0/Envoy Tech Lab/Repositories/SENTRY_DASHBOARD_HOLISTIKA.md) (per the I66 P5 prior work). Verify `next.config.mjs withSentryConfig` carries `sourcemaps: { disable: process.env.VERCEL_ENV !== 'production' }`. If anything is missing or drifted, open a PR titled `chore(observability): I68 P4 — adopt canonical Sentry release-format + skip-on-preview source maps`.
2. Open the `hlk-erp` working tree. Apply the §3 Next.js Vercel template verbatim (substituting `<repo-slug>` → `hlk-erp`); add the `next.config.mjs withSentryConfig` `sourcemaps.disable` clause if missing. Open a PR titled the same way.
3. Open the `kirbe-platform` working tree. Apply the §3.2 Python FastAPI template verbatim (substituting `<repo-slug>` → `kirbe-platform`). Open a PR titled the same way.
4. Once all 3 PRs are merged, paste the §4 dashboard YAML block into the Sentry UI dashboard editor (Sentry → Dashboards → Create New → "Holistika deploy-health (cross-repo)"). Wait 24h. Verify all 3 platform repos appear in widget 1 ("Deploy success rate per repo (rolling 7d)") with non-zero release events.
5. Once verified, the operator (or agent in a future session with sibling-repo access) sets `AKOS_SENTRY_RELEASE_SCAN_CONSUMERS=1` and re-runs `py scripts/release-gate.py` to verify consumer-scan now passes against the populated sibling-repo configs. At that point D-IH-68-C + D-IH-68-G + D-IH-68-I can be moved from `proposed`/`interim` → `decided` in [`decision-log.md`](../decision-log.md), and `SENTRY_DASHBOARD_HOLISTIKA.md` `status:` flips from `review` → `active` in P8 closure.

## Cursor-rules adherence summary

- [`.cursor/rules/akos-governance-remediation.mdc`](../../../../.cursor/rules/akos-governance-remediation.mdc) — one-commit-per-phase honoured (P4 commit ships all AKOS-side files in a single phase-scoped commit; sibling-repo PRs are operator follow-up); reuse-only (extends `akos/`, `scripts/release-gate.py`, `config/verification-profiles.json`, `tests/`, `pyproject.toml` markers — no parallel framework introduced); HLK compliance asset classification followed.
- [`.cursor/rules/akos-planning-traceability.mdc`](../../../../.cursor/rules/akos-planning-traceability.mdc) — plan-quality bar honoured (this pause record is multi-section, file-path-dense, decision-log-aware, risk-aware, has explicit "what's blocked and why" prose).
- [`.cursor/rules/akos-agent-checkpoint-discipline.mdc`](../../../../.cursor/rules/akos-agent-checkpoint-discipline.mdc) — mid-phase agent self-checkpoint filed (not an operator pause-point; P4 has no explicit pause-point per the Round-2 plan §P4 "no pause point").
- [`.cursor/rules/akos-mirror-template.mdc`](../../../../.cursor/rules/akos-mirror-template.mdc) — AKOS canonical doc + Pydantic + validator + tests are SSOT; sibling-repo Sentry inits mirror; agent does NOT invent sibling-repo PRs from this workspace.
- [`.cursor/rules/akos-docs-config-sync.mdc`](../../../../.cursor/rules/akos-docs-config-sync.mdc) — new doc-sync row added for the canonical Sentry dashboard doc → all downstream surfaces.
- [`CONTRIBUTING.md`](../../../../CONTRIBUTING.md) §"Python Code Standards" + §"Testing Standards" — Pydantic models with field validators + type hints + structured logging via `akos.log` + cross-platform pathlib via `akos.io.REPO_ROOT` + valid/invalid test pairs all honoured.
