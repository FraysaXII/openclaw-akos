# Initiative 24 — Decision Log

| ID | Date | Decision | Rationale | Status |
|:---|:-----|:---------|:----------|:-------|
| **D-IH-10** | 2026-04-29 | **Methodology has FOUR layers**: brand foundation + concept + use-case + eloquence. Brand-craft is real and pre-exists; we don't invent it from CSVs. Eloquence operates **within** brand voice, not parallel to it. SOP `SOP-HLK_COMMUNICATION_METHODOLOGY_001.md` lives under `v3.0/Admin/O5-1/Marketing/Brand/` (CMO chain). `process_list.csv` row added under `thi_mkt_prj_1` as a NAMED tranche "Communication methodology maintenance" (G-24-2). | Brand-craft is pre-existing knowledge; methodology layers it on canonical CSV facts and per-channel templates. Concept (CSVs) → Use-case (recipient profile) → Eloquence (voice register) all operate inside the brand voice envelope. | **APPROVED** |
| **D-IH-11** | 2026-04-29 | **Recipient voice profile** = extend `GOI_POI_REGISTER.csv` with optional `voice_register`, `language_preference`, `pronoun_register` columns. Composer reads these + falls back to discipline default + brand foundation default. **The CSV column add IS a mirror DDL ALTER (G-24-1)** because `compliance.goipoi_register_mirror` exists. Staging `scripts/sql/i24_phase1_staging/<ts>_i24_compliance_goipoi_register_mirror_alter_{up,rollback}.sql` + `supabase/migrations/<ts>_i24_compliance_goipoi_register_mirror_alter.sql`; apply via MCP. | No new CSV — reuse existing GOI/POI dimension. Mirror ALTER is non-destructive (additive nullable columns); rollback drops the columns. | **APPROVED** |
| **D-IH-17** | 2026-04-29 | **Brand foundation prerequisite** — the brand manager has lived protocols (voice charter, archetype, narrative pillars, do/don't matrices, register-by-relationship). Initiative 24 P0a discovers and documents them under `v3.0/Admin/O5-1/Marketing/Brand/` BEFORE the methodology SOP is finalized. The SOP **cites** the foundation; we do **not** invent brand craft from CSVs. The CSV/governed metadata layer adds **discoverability + propagation + drift detection** to existing brand-craft, not replacement. | Operator's brand voice is the source of truth. Inventing brand voice from canonical data violates the operator's lived expertise. The governed layer's value is making brand voice **traceable, propagated, and drift-detected** across composer outputs. | **APPROVED** |
| **D-IH-24-A** | 2026-04-29 | **Brand foundation MDs ship as scaffolds with `status: scaffold-awaiting-discovery`** until the operator fills YAML Section 2. Validator emits **one warning per file** (not 48 errors per cell). The methodology SOP cites the (scaffolded) foundation; once the operator fills the YAML and the scaffolder writes real content, the `status` flips to `active` and the SOP citations become live. | Unblocks Initiative 24 P1 + P2 + P4 + P5 from operator-interview gating; structural artifacts and discovery contract land now, content follows when operator is ready. Per Wave-2 plan T3 tier: "Defer until execution surfaces it" is too pessimistic for the SOP+composer track; structure-now/content-later is the right balance. | **APPROVED** |

## Re-evaluation triggers

- **D-IH-10** Layer count: review when communication needs evolve (e.g. multi-modal voice/video adds a 5th layer, or AI-assisted message generation needs explicit guardrails as a 5th).
- **D-IH-11** Voice columns: review when operator surfaces a 4th voice axis (formality?  warmth?) the current 3 don't capture.
- **D-IH-17** Brand foundation: review annually (Brand Manager-led) — voice/tone/archetype/pillars evolve with the company.
- **D-IH-24-A** Scaffold→active flip: target by 2026-05-15 or before composer ships the first real-recipient message.
