---
language: en
status: active
initiative: 59-hlk-governance-clean-slate
report_kind: evidence-matrix
program_id: shared
plane: ops
authority: Founder
last_review: 2026-05-06
---

# Initiative 59 — Evidence matrix

| ID | Observation | Source | Impact |
|:---|:------------|:-------|:-------|
| **E1** | I58 closed engineering AND operator-side 2026-05-06 with all engineering targets met (1741 tests passing; release-gate 8/8; live A.1 + A.2 calibration burns + MADEIRA dossier emit + GraphRAG NO-GO + cost ≤ $0.50). Three residuals forwarded as OPS items (OPS-58-2 OpenAI key rotation, OPS-58-3 rubric calibration fix, OPS-58-4 GraphRAG live wiring). | [I58 master-roadmap](../58-cycle-2-multi-track-forward/master-roadmap.md) header + closure UAT addendum 2026-05-06 | I59 inherits a drift-clean baseline. The three OPS-58-* items are highest-RICE forward-list candidates; OPS-58-3 closes engineering-side inside I59 P6 (RICE 149). |
| **E2** | Planning workspace shows ~50 initiatives across the planning README. Status semantics drift across files: I02 frontmatter says `status: active` but its closure conditions were met by I52 P3 (judge MVP shipped); I04/I08/I14/I22 are program-line items running on cadence; I06/I55 are continuous loops; I12/I56 are gated on external events; I24 is gated on operator content. WIP_DASHBOARD lumps all into "Active" or "Open". | `docs/wip/planning/README.md` + `docs/wip/planning/WIP_DASHBOARD.md` rows 02–58 + per-initiative master-roadmap.md frontmatter snapshot 2026-05-06 | The seven-value taxonomy enum (`closed`/`archived`/`active`/`continuous`/`program_line`/`gated_external`/`gated_operator`) directly addresses the operator's "what's truly active vs deferred" question. |
| **E3** | Eleven HLK compliance dimensions exist as canonical CSVs: `process_list.csv` (1100 rows), `baseline_organisation.csv` (65 rows), `PERSONA_REGISTRY.csv` (16 rows), `CHANNEL_TOUCHPOINT_REGISTRY.csv` (10 rows), `SOURCING_REGISTER.csv`, `GOI_POI_REGISTER.csv`, `SKILL_REGISTRY.csv`, `POLICY_REGISTER.csv`, `TOPIC_REGISTRY.csv` (28 rows), `PERSONA_SCENARIO_REGISTRY.csv` (329 rows), `KM_TOPIC_FACT_SOURCE.csv`. Each has Pydantic schema + validator + Supabase mirror + KM manifest + PRECEDENCE row. The pattern has been reused 11× already; marginal cost of adding 5 more is low. | `docs/references/hlk/compliance/dimensions/` + `docs/references/hlk/compliance/` + `scripts/validate_*.py` + `compliance_mirror_emit` profile + `PRECEDENCE.md` | Five new dimensions (REPOSITORY/INITIATIVE/OPS/CYCLE/DECISION) follow the same template; ~1500 LOC across 5 schemas + 8 validators + 5 migrations + tests. |
| **E4** | Existing markdown SSOT for repos is `docs/references/hlk/v3.0/Envoy Tech Lab/Repositories/REPOSITORIES_REGISTRY.md` (markdown table; cited in PRECEDENCE.md as canonical). It tracks 5 repos: `openclaw-akos` (this), `madeira-platform`, `holistika-deck`, `holistika-deck-mirror`, `holistika-cf-edge`. The markdown table has columns: repo_slug, repo_url, role, supabase_org_id, default_branch, license, sync_with_main_repo. | Reading the file directly | I59 P1.1 promotes the markdown table to a CSV-backed dimension; sync validator asserts both stay aligned. |
| **E5** | Three sources of OPS-XX-Y items exist with **no governed schema**: (a) per-initiative master-roadmap.md `Forwarded residuals` sections; (b) `CHANGELOG.md` "OPS-XX-Y forwarded" entries; (c) ad-hoc agent-summary mentions. The forward list documented at the I58 closure includes: OPS-54-1.c, OPS-55-1, OPS-56-1, OPS-57-1, OPS-58-1, OPS-58-2, OPS-58-3, OPS-58-4, OPS-14-1, OPS-50-1, OPS-51-1, OPS-44-1, OPS-32-1, OPS-30-1. ~16 active OPS items total. | Cross-reference grep across `docs/wip/planning/**/master-roadmap.md` + `CHANGELOG.md` + last 5 closure UAT reports | I59 P1.3 mints `OPS_REGISTER.csv` with one row per OPS item; P3 seeds the ~16 known items; P4 OPERATOR_INBOX.md auto-renders from the operator-class subset. |
| **E6** | Three coordinating cycles exist (I57, I58, I59) but have no governed registration. They differ structurally from regular initiatives: they coordinate multiple sub-initiatives via FK joins (I58 coordinated I28/I29/I30/I31). Existing cycle-coordinated initiatives like `coordinated_initiative_ids` are tribally-known. | `docs/wip/planning/57-*/master-roadmap.md` + `docs/wip/planning/58-*/master-roadmap.md` + I59 (this) | I59 P1.4 mints `CYCLE_REGISTER.csv`; P3.7 seeds CYC-57 + CYC-58 + CYC-59 (4 rows; one per closed/active cycle). |
| **E7** | Decision IDs (D-IH-XX-Y) are referenced in 47 distinct places across initiative decision-log.md files. They follow a stable pattern: `D-IH-{INITIATIVE_NUM}-{LETTER}`. Each decision has: title, status, alternatives, rationale, reversibility. Cross-references exist (e.g., D-IH-58-C cites D-IH-46-E + D-IH-53-C). No queryable index exists. | `rg "D-IH-\d+-[A-Z]" docs/wip/planning/**/decision-log.md` ≈ 47 decisions | I59 P1.5 mints `DECISION_REGISTER.csv` with one row per decision; P3 seeds best-effort audit (~30 rows from active initiatives + I57/I58 closure-cycle decisions). Cross-references become real FKs. |
| **E8** | OPS-58-3 evidence (from I58 OPS-58-1 live execution): A.1 OPERATOR cassette n=50 returned `persona_fit` alignment 0%; A.2 PERSONA-INVESTOR-COLD cassette n=35 returned 5.7%. Root cause: `_heuristic_persona_fit(persona=None, ...)` returns 3 (default) when `persona` argument is None, while live judges return calibrated scores (4-5). Cassette dispatch passes `scenario` dict but burn harness doesn't resolve `scenario["persona_id"]` → PERSONA_REGISTRY → persona dict. RICE = 149. | `docs/wip/planning/58-cycle-2-multi-track-forward/reports/ops-58-1-2026-05-06.md` "A.1 + A.2 alignment matrix" | I59 P6 fixes path A: resolve persona dict from scenario.persona_id in the burn harness; pass full persona to `JudgeRoster.score()`. Re-run A.1+A.2 burns; expect ≥80% alignment. |
| **E9** | Telemetry promotion proposals folder exists at `artifacts/telemetry-proposals/` (gitignored) with `README.md` documenting "Auto-merge to PERSONA_SCENARIO_REGISTRY.csv is forbidden by design (D-IH-17 operator-content invariance)." Last operator merge was 2026-04-22 (I49 P11 close). Telemetry has accumulated ~30 days of unmerged signals. | `artifacts/telemetry-proposals/README.md` + I50 P5 closure UAT | I59 P7 runs `scripts/promote_telemetry_to_scenario.py --since-days 30` (in operator's stead), produces triage report, mints OPS-59-1 row pointing operator at the merge step. |
| **E10** | The user explicitly requested in the I59 framing: "Remember that you are the operator assistant, so there are things you can do in my stead." This unblocks routine engineering-side work (telemetry triage, OPS-58-3 fix, status audit, registry seeding) that previously waited on operator-arbitrated kickoff. Operator-content (process_list mints, OpenAI key rotation, SOP `status: active` flip) stays gated. | Operator framing 2026-05-06 (verbatim) | The line between "in stead" and "operator-content-gated" is precise: agent runs scripts + drafts proposals + flips engineering-side status; operator approves G-59-A/B/C/D batches + ratifies SOPs + mints `process_list.csv` + rotates keys. |
| **E11** | Existing rules `.cursor/rules/akos-governance-remediation.mdc` (canonical CSV gates, commit and phase discipline) + `.cursor/rules/akos-mirror-template.mdc` (precedence, brand-jargon, never-invent-IDs) + `.cursor/rules/akos-planning-traceability.mdc` (UAT vs automated smoke) + `.cursor/rules/akos-docs-config-sync.mdc` (docs-config sync triggers) are all "always-applied". They define the rulebook I59 must follow without exception. | `.cursor/rules/akos-*.mdc` (5 files; all `alwaysApply: true`) | I59 plan, commits, and PRs all comply. New SOPs follow the v3.0 vault structure. CSV mints carry G-59-A approval. Process_list does NOT receive mints. |
| **E12** | The I50 P5 telemetry promotion script (`scripts/promote_telemetry_to_scenario.py`) is operational; takes `--since-days N` flag; produces JSON proposals under `artifacts/telemetry-proposals/`. It does NOT modify `PERSONA_SCENARIO_REGISTRY.csv` directly. Operator merge is via separate manual step. | `scripts/promote_telemetry_to_scenario.py` source + I50 P5 closure UAT | P7 runs cleanly without operator coordination; output is review-ready. |
| **E13** | The I52 multi-judge dispatcher (`akos/eval_harness/judge.py`) was wired live during I58 OPS-58-1 (D-IH-58-I). Anthropic + OpenAI SDKs added to `requirements.txt`. `JudgeRoster.score()` includes `fallback-reasons` in `notes` field. Live cost tracking via `_JUDGE_PRICING_USD_PER_MTOK`. | I58 closure UAT addendum 2026-05-06 + judge.py changes | I59 P6 modifies the offline `_heuristic_persona_fit` path only — does not touch the live API path. Re-run A.1+A.2 burns will use both: live judges for ground truth (Anthropic-only roster per D-IH-58-I; OpenAI rotation deferred to OPS-58-2); offline rubric for alignment comparison. |
| **E14** | The forward list at I58 closure includes OPS-58-2 (OpenAI key rotation; RICE 50; operator 1-min paste; minimum operator action). Until rotation: `AKOS_JUDGE_ROSTER` stays `anthropic:claude-sonnet-4-5,anthropic:claude-haiku-4-5` (in-family fallback). I59 P6 re-run uses the same in-family roster. | I58 closure UAT addendum + `scripts/preflight_g58_1.py` D-IH-58-I logic | P6 re-run does NOT block on OPS-58-2; in-family roster is sufficient to validate rubric calibration lift. |
| **E15** | Operator framing 2026-05-06 included: "Decide the best-effort" + "go all out, I want a clean slate" + "you are the operator assistant" + "extend with similar standpoint as DECISION_REGISTER" + "All in one initiative or put all planned initiatives in this plan, you choose". Operator delegates initiative-coordination scope decisions to the agent within the always-applied governance rules. | Operator messages 2026-05-06 (verbatim) | I59 ships in one cycle with five dimensions + two SOPs + clean-slate audit + OPS-58-3 fix + telemetry routine + harmonisation proposal. I60/I61 are forward candidates listed in the planning README. |
| **E16** | The two-layer SSOT pattern is already proven in HLK: `docs/references/hlk/v3.0/**/*.md` (canonical SOPs) ↔ `docs/references/hlk/_assets/**/*.manifest.md` (KM manifests as governed metadata) ↔ `KM_TOPIC_FACT_SOURCE.csv` (cross-cuts). Sync validators (`validate_hlk_km_manifests.py`) catch drift. | `scripts/validate_hlk_km_manifests.py` + KM_TOPIC_FACT_SOURCE structure | I59 reuses the pattern: master-roadmap.md ↔ INITIATIVE_REGISTRY.csv via new `validate_initiative_registry_frontmatter_sync.py`; decision-log.md ↔ DECISION_REGISTER.csv via new `validate_decision_register_decision_log_md_sync.py` (advisory). |
| **E17** | I57 closure shipped 1764 passed / 7 skipped; I58 closure shipped 1741 passed / 8 skipped (test count slightly down due to Phase A cassettes being run live). Target for I59 closure: 1741 baseline + ~95 new tests = ≥1836 (5 dimensions × ~10 tests each + 2 sync validators × ~5 + status taxonomy ~5 + dashboard renderer ~5 + inbox renderer ~5 + freshness canary ~5 + persona-fit fix ~5 + frontmatter validator ~5). | I58 closure UAT addendum verification matrix | P10 verification matrix expects ≥1836 tests; deviation > 5% triggers post-closure investigation. |
