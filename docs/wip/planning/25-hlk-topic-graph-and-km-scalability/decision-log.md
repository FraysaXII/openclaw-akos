# Initiative 25 — Decision Log

| ID | Date | Decision | Rationale | Status |
|:---|:-----|:---------|:----------|:-------|
| **D-IH-12** | 2026-04-29 | **Topic graph SSOT — CSV is dimension authority, Neo4j is graph projection.** New canonical CSV `docs/references/hlk/compliance/dimensions/TOPIC_REGISTRY.csv` is the **single source of truth** for cross-topic edges (`parent_topic`, `depends_on`, `related_topics`, `subsumes`, `subsumed_by`). The manifest under `_assets/<plane>/<program_id>/<topic_id>/*.manifest.md` is a **per-topic projection** that **must FK-resolve** into `TOPIC_REGISTRY.csv`; `validate_hlk_km_manifests.py` enforces the FK and never authors. **Drift = canonical wins** per `PRECEDENCE.md`. The Postgres mirror stores text columns (DAMA-pure mirror); **Neo4j projection adds typed relationship edges** (`:DEPENDS_ON`, `:TOPIC_PARENT_OF`, `:RELATED_TO`, `:TOPIC_SUBSUMES`) for graph queries. Three-step rendering chain: `scripts/render_topic_graph.py` reads the CSV → emits `_assets/_meta/topic_graph.{mmd,png,svg}`. Obsidian `[[wikilinks]]` adopted as **secondary** nav inside topic companion `.md` files **and explicitly OUT OF SCOPE for `validate_hlk_vault_links.py`**. | Same pattern as Initiative 23 (PROGRAM_REGISTRY): a separate canonical CSV under `compliance/dimensions/` provides cross-topic edges without coupling per-topic manifests. Neo4j projection makes multi-hop graph queries first-class. | **APPROVED** |
| **D-IH-13** | 2026-04-29 | **`.mmd`-first P0 discipline** — any initiative **producing or modifying a Topic** must commit the `<topic>.mmd` source-of-truth in P0 alongside `master-roadmap.md`. Initiatives that only **consume** an existing Topic do not need a new `.mmd`. | The Mermaid source is the canonical structure; PNG/SVG are derived. Locking the `.mmd` in P0 prevents the "we'll add the visual later" trap that produced Initiative 21's placeholder PNG (which Initiative 22 P5 had to retroactively replace). | **APPROVED** |
| **D-IH-18** | 2026-04-29 | **Neo4j Topic projection extends Initiative 23 P-graph** — `:Topic` nodes + edges (`:CONSUMES`, `:PRODUCES_FOR`, `:PROGRAM_PARENT_OF`, `:PROGRAM_SUBSUMES` from Initiative 23 + new `:DEPENDS_ON`, `:TOPIC_PARENT_OF`, `:RELATED_TO`, `:TOPIC_SUBSUMES`, `:UNDER_PROGRAM`). Generalize `:OWNED_BY` to also accept `:Topic` source. Edge naming disambiguated to coexist with the existing `:PARENT_OF` (process tree) and Initiative 23's `:PROGRAM_PARENT_OF`. | Multi-hop topic dependency / parent navigation belongs in a graph; CSVs + Postgres mirrors hold full functionality without graph queries (graceful SKIP when Neo4j unconfigured). | **APPROVED** |
| **D-IH-25-A** | 2026-04-29 | **TOPIC_REGISTRY.csv seed scope** — Initiative 25 P2 seeds the two existing topics: `topic_external_adviser_handoff` (Initiative 21 + relocated by Initiative 22 P2 to `_assets/advops/PRJ-HOL-FOUNDING-2026/adviser_handoff/`) and `topic_kirbe_billing_plane_routing` (Initiative 23 P6, under `_assets/techops/PRJ-HOL-KIR-2026/`). ENISA backfill (P6) adds a third (`topic_enisa_evidence`) with `depends_on=topic_external_adviser_handoff`. Brand voice topic (`topic_brand_voice` from Initiative 24) is added when the brand foundation MDs flip from scaffold to active. | Seed only what already exists in `_assets/`; bigger backfill is wave-3 work. | **APPROVED** |
| **D-IH-25-B** | 2026-04-29 | **Topic edge naming** — `:DEPENDS_ON`, `:TOPIC_PARENT_OF`, `:RELATED_TO`, `:TOPIC_SUBSUMES`. Disambiguates from Initiative 23's `:CONSUMES` / `:PROGRAM_PARENT_OF` / `:PROGRAM_SUBSUMES` (Program-side) and Initiative 07's `:PARENT_OF` (process tree). | Each entity-tree's edges have prefixed types where collision is possible; `:RELATED_TO` is unprefixed because it is unique to topics today. | **APPROVED** |

## Re-evaluation triggers

- **D-IH-12** review when topic count exceeds 100 (current CSV-row-by-row load may be too slow for the validator).
- **D-IH-13** review when initiatives commonly modify > 5 topics — batch-render-first may be cheaper than per-topic `.mmd` discipline.
- **D-IH-18** Neo4j promotion to mandatory — Initiative 26 P0 re-eval-trigger template `re-eval-trigger-graph-mcp-tooling-promotion.md`.
- **D-IH-25-A** seed scope expansion — wave-3 will backfill remaining v3.0 topics into TOPIC_REGISTRY.
