# I53 / P3 — A/B run report (NO-FIRE this cycle)

**Date:** 2026-05-03
**Phase:** P3 (A/B run + report; G-53-1 input)
**Outcome:** **NO-FIRE** — live A/B execution forwarded as `OPS-53-1` to next AKOS_RECORD_LIVE cycle.

## Why no-fire

Per **R-46-1**, the live A/B requires:

1. ~$20 operator-funded LLM API budget.
2. Operator confirmation of three things in their environment:
   - `pip install neo4j-graphrag-python` (optional dependency per R-46-11; not in `requirements.txt`).
   - `AKOS_GRAPHRAG_POC_LIVE=1` env var explicitly set.
   - Provider env keys (`OPENAI_API_KEY` / `ANTHROPIC_API_KEY`) for the retrieval-LLM and the I52 multi-judge accuracy scoring.

This cycle's master roadmap message did not include those three operator confirmations; the cursor session also does not have outbound API-call capability to spend on operator-funded budget. **R-53-4 anticipated and accepted this outcome:** the no-fire is a governance event, mirroring the I52 P3 / P4 / P5 stub-mode pattern (e.g., I52 P3 calibration burn returned 100% / 100% / 100% in dispatcher-validation mode because no real API was called).

## What this means downstream

Per **D-IH-53-C** (non-additive trade-off thresholds), G-53-1 cannot fire without concrete A/B numbers on at least one of the three bars:

1. ≥3pp accuracy lift over baseline `hlk_role + hlk_search` chain.
2. ≥30% latency reduction (median).
3. ≥40% cost reduction (per query, including indexing amortisation).

**No-fire ⇒ no-ship for this cycle.** P5 (CSV flip + POLICY clone) does not execute. P4 logs the no-ship verdict.

## What still ships from I53

The no-fire outcome does **not** prevent I53 from delivering its non-live contract:

- **P0 governance scaffold** — six artefacts under `53-graphrag-poc-closure/`.
- **P1 golden-set audit** — 20 queries governed; pre-flight cost estimate ~$10/$20 (R-53-1 mitigated).
- **P2 PoC infra audit** — `--validate-config` + `--dry-run` PASS; live-mode env-guard PASS.
- **P3 (this report)** — no-fire governance event; OPS-53-1 forwarded.
- **P4** — D-IH-46-Decision-P3-NO-SHIP-2026-05-03 row in `46-neo4j-strategic-posture/decision-log.md`.
- **P6** — `neo4j_governance_kg_drift_smoke` profile + adversarial probes verified.
- **P7** — I46 + I53 closure on the no-ship path; cassettes preserved for next cycle.

## OPS-53-1 (forwarded)

| Field | Value |
|:--|:--|
| ID | OPS-53-1 |
| Class | operator-funded live execution |
| Description | First live GraphRAG PoC A/B run on `SKILL-MADEIRA-LOOKUP-V1` (20 queries × 2 modes; multi-judge accuracy scoring via I52 roster); ~$10-15 estimated spend within R-46-1 $20 envelope |
| Trigger | Next operator-funded `AKOS_RECORD_LIVE=1` window (likely shared with **OPS-52-1** real-API multi-judge calibration burn — one operator-funded cycle for both) |
| Owner | System Owner |
| Acceptance | `reports/graphrag-poc-results-YYYY-MM-DD.md` published with concrete cost / latency / accuracy delta per query class; D-IH-46-Decision-P3-{SHIP\|NO-SHIP}-YYYY-MM-DD logged in `46-neo4j-strategic-posture/decision-log.md` |
| Reversal | If P4 verdict is SHIP, P5 flips `SKILL-MADEIRA-LOOKUP-V1.retrieval_mode` to `graph_rag` + clones `pol_neo4j_graph_rag_eligibility` per topic class; rollback path D-IH-53-D |

## Verification

This phase's purpose is the governance event itself. There is no test target for "no live API call was made"; the absence is the report. The downstream phases (P4-P7) honour the no-fire branch.
