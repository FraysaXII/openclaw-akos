# Phase 3b — MCP / agent integrations (deferred)

**Rule:** No broad **Cursor-only** MCP as end state. After **human runbooks** + connector registry + RLS:

- Add agent-facing MCP tools aligned with `config/mcporter.json.example`.
- Read-only ad queries where approved; **no** duplicate credentials outside Supabase RPCs.

**Status:** Deferred until Phase 3 stack and event taxonomy [`env_tech_dtp_243`](../../../references/hlk/compliance/process_list.csv) are stable.
