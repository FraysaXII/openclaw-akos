---
language: es
status: draft
audience: Operator (boilerplate es reference-only; sin equipo)
delivery_method: nota corta junto al parche
---

**Asunto:** [Holistika Ops] boilerplate — registro ligero como reference (1 fichero)

Hola,

Registro ligero del repo boilerplate como parte de Initiative 32 P11 (D-IH-32-N).

**1 fichero a aterrizar** — `EXTERNAL_REPO_CONTRACT.md` en la raíz del repo. Eso es todo. El repo boilerplate no tiene directorio `.cursor/rules/` así que no se envía espejo de regla de cursor — por diseño, según D-IH-32-N (referencia ligera).

**Lo que esto cambia:** boilerplate queda `class=reference` en `REPOSITORIES_REGISTRY.md` y rastreado por la instantánea semanal `REPO_HEALTH_SNAPSHOT.csv` (aparece con `has_external_repo_contract=true` tras aterrizar el fichero). Sin obligación SSOT. Los activos de marca (logo SVG, degradado del hero) quedan como referencia visual según `BRAND_VISUAL_PATTERNS.md` en AKOS.

**La instantánea Obsidian embebida en** `app/dashboard/applications/kms/obsidian-holistika-main/` **queda como archivo histórico** — explícitamente NO canónica. Initiative 43 (diferida) la reemplazará por un puntero a AKOS `docs/references/hlk/v3.0/` cuando el operador migre completamente su flujo Obsidian personal fuera de boilerplate.

Sin más acción necesaria.

— Holistika AKOS governance
