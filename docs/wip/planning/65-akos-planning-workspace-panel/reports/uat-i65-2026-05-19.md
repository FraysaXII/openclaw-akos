---
intellectual_kind: uat_report
parent_initiative: INIT-OPENCLAW_AKOS-65
sharing_label: internal_only
authored: 2026-05-19
last_review: 2026-05-19
linked_decisions:
  - D-IH-86-AQ  # Wave I Lane I-D closure decision
language: en
status: implementation_complete_walkthrough_deferred
audience: J-OP
role_owner: System Owner
co_owner_role: PMO
---

# I65 UAT — implementation walk

> **Status (2026-05-19):** Implementation **complete** for both AKOS backend and `hlk-erp` UI. Live operator walkthrough **deferred** because the `hlk-erp` commit `398d9db` is staged locally but **not pushed** — push gated on operator ratification of two pre-existing local commits in `hlk-erp/main` (`cb19f81` merge + `31df5c5` I66 P0 carry-over) that pre-date this Wave. This UAT report documents the implementation surface and the walkthrough checklist; the dated UAT outcomes table will be filled in after the deferred push lands and a sign-in session walks the live routes.

## 1. What was shipped

### 1.1 AKOS side (commit `62ea95a`)

- [`supabase/migrations/20260519220000_i65_p4_governance_planning_atlas_views.sql`](../../../../../supabase/migrations/20260519220000_i65_p4_governance_planning_atlas_views.sql) — two read-only views over the existing compliance mirrors:
  - `governance.planning_decisions_view` joins [`compliance.decision_register_mirror`](../../../../../supabase/migrations/20260508010100_i65_p1_governance_planning_workspace.sql) with [`compliance.initiative_registry_mirror`](../../../../../supabase/migrations/20260508010100_i65_p1_governance_planning_workspace.sql) on `initiating_initiative_id`.
  - `governance.planning_ops_view` joins [`compliance.ops_register_mirror`](../../../../../supabase/migrations/20260506120200_i59_ops_register_mirror.sql) with the same initiative table on `originating_initiative_id`.
- [`reports/sql-proposal-i65-p4-2026-05-19.md`](sql-proposal-i65-p4-2026-05-19.md) — the operator-SQL-gate proposal that ratified Option A (both views with full regression).
- Both views inherit RLS from underlying mirrors (deny `anon` + `authenticated`; `service_role` reads). No GRANT changes beyond `GRANT SELECT TO authenticated, service_role`.

### 1.2 hlk-erp side (commit `398d9db` LOCAL — push deferred)

P3 — drill-in components in `app/(operator)/planning/[slug]/page.tsx`:

- `components/planning/decision-timeline.tsx` — vertical timeline with status pill + class chip + reversibility chip + log link.
- `components/planning/evidence-checklist.tsx` — markdown table parser → status-grouped tabular reading (passed / blocked / deferred / open / other).
- `components/planning/risk-card-list.tsx` — markdown table parser → likelihood × impact card grid grouped high/medium/low.
- `components/planning/cross-link-rail.tsx` — shared-topic_id navigation chips computed from the existing `fetchPlanningInitiatives` pull.

P4 — three new routes:

- `/planning/decisions` — Decision Atlas filterable by status + class; reads `governance.planning_decisions_view` via `fetchPlanningDecisionsAtlas`.
- `/planning/operations` — Ops Queue RICE-sorted with operator / engineering / mixed owner-class filter; reads `governance.planning_ops_view` via `fetchPlanningOpsQueue`.
- `/planning/reports` — Reports Stream reverse-chronological feed via `fetchPlanningReportsStream` (concurrent GitHub Contents API listings; no SQL view).

E2E tests: `tests/e2e/planning-drilldown.spec.ts` + `tests/e2e/planning-atlas-stream-queue.spec.ts` — anonymous-redirect smoke for the five planning routes (authenticated assertions live in operator-only suites against staged Supabase + `GH_PAT_PLANNING_READER`).

## 2. Walkthrough checklist (to fill in when hlk-erp push lands)

Each row is `step | result | notes`. Walk the routes after `git push` of `hlk-erp/main` lands and the deploy hits production-equivalent (Vercel preview is fine for sign-in walkthrough; production for sealed integrity).

| # | Step | Expected | Result | Notes |
|:-|:-|:-|:-|:-|
| 1 | Sign in at `/sign-in` with operator credentials | Reaches `/mission-control` | TBD | |
| 2 | Navigate `/planning` | Index renders with active initiative count + status filter | TBD | |
| 3 | Click `86-initiative-cluster-execution-coordinator` row | Drill-in renders master-roadmap MDX + decision timeline + evidence checklist + risk card list + cross-link rail | TBD | |
| 4 | Verify decision timeline ordering | Newest decision at top, reversibility + status pills correct | TBD | |
| 5 | Verify evidence checklist parses [`evidence-matrix.md`](../evidence-matrix.md) | Status groups populated (passed / open / etc.) | TBD | |
| 6 | Verify risk card list parses [`risk-register.md`](../risk-register.md) | Cards grouped high/medium/low by likelihood × impact | TBD | |
| 7 | Verify cross-link rail surfaces topic-shared initiatives | Chips link to other initiatives sharing topic_ids | TBD | |
| 8 | Navigate `/planning/decisions` | Decision Atlas renders ≥330 decisions; filter by class=closure shows D-IH-86-AQ + ancestors | TBD | |
| 9 | Filter Decision Atlas by status=active | Only active rows show; superseded hidden | TBD | |
| 10 | Navigate `/planning/operations` | Ops Queue renders active actions sorted by RICE descending | TBD | |
| 11 | Filter Ops Queue by owner_class=operator | Only operator-class actions show | TBD | |
| 12 | Navigate `/planning/reports` | Reports Stream renders reverse-chrono; UAT/pause-record/blocker classification visible | TBD | |
| 13 | Filter Reports Stream by kind=uat | Only UAT reports show; this report appears in the list | TBD | |
| 14 | Click an `uat-*` row → opens GitHub link | Markdown rendered on GitHub for the file | TBD | |

**Deferred until `hlk-erp/main` is pushed past `398d9db`.** When the push lands, walk the table above, fill in PASS / SKIP / N/A per row, and append a `## 3. Outcomes` section with the dated row results.

## 3. Outcomes (to be filled in)

> Reserved for the dated walkthrough outcomes table after the deferred push lands. Per the AKOS planning-traceability rule UAT evidence contract, the closure of I65 to `closed` status in [`INITIATIVE_REGISTRY.csv`](../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/INITIATIVE_REGISTRY.csv) is gated on this section being populated.

## 4. Cross-references

- I65 master-roadmap: [`master-roadmap.md`](../master-roadmap.md)
- AKOS Wave I closure: [`docs/wip/planning/86-initiative-cluster-execution-coordinator/master-roadmap.md`](../../86-initiative-cluster-execution-coordinator/master-roadmap.md) §1.7
- D-IH-86-AQ closure decision: [`DECISION_REGISTER.csv`](../../../docs/references/hlk/v3.0/Admin/O5-1/People/Compliance/canonicals/DECISION_REGISTER.csv)
- hlk-erp commit (local): `398d9db` (`I65 P3+P4 — planning drilldown components + atlas/queue/stream routes`)
- AKOS commit: `62ea95a` (`I86 Wave I Lane I-D.1: I65 P4 atlas+queue governance views`)
- Sibling pre-existing hlk-erp commits gating the push:
  - `31df5c5` — I66 P0 carry-over (sub-mark phrasing fix + BASELINE_REALITY.md scaffold)
  - `cb19f81` — Merge from origin/main
- akos-planning-traceability.mdc UAT evidence contract: this file follows the `uat-<topic>-<YYYYMMDD>.md` naming pattern; outcomes table will populate at deferred-push-+-walkthrough time.
