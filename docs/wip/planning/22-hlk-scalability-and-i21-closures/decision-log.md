# Initiative 22 — Decision Log

| ID | Date | Decision | Rationale | Status |
|:---|:-----|:---------|:----------|:-------|
| **D-IH-1** | 2026-04-29 | **Refactor strategy = Hybrid-light.** Convention-only for `compliance/` CSV physical moves; physical restructure of `_assets/`; program-subfolder pattern under `v3.0/<role>/programs/<program_id>/`. | Full physical move of `compliance/*` would cascade through ~25 files (akos modules, validators, sync script, `PRECEDENCE.md`, mirror DDL, SOPs, tests) for limited near-term benefit. `_assets/` and `v3.0/` are the actively-growing surfaces and benefit immediately. Compliance-folder move is reserved for a dedicated initiative when the FINOPS / COMPONENT trees expand. | **Approved (operator default)** |
| **D-IH-2** | 2026-04-29 | **Plane × Program × Topic** as the canonical layout axis for `_assets/<plane>/<program_id>/<topic_id>/`. | Aligns with how new operating planes are added (ADVOPS row in `akos-holistika-operations.mdc`); `program_id` already exists on every Initiative-21 register; `topic_id` is the existing KM unit per `HLK_KM_TOPIC_FACT_SOURCE.md`. Three axes are sufficient — deeper trees are role/topic concerns, not asset layout. | **Approved** |
| **D-IH-3** | 2026-04-29 | **KM Output-1 source-of-truth = Mermaid `.mmd` + rendered PNG/SVG via `mmdc`.** Manifest gains an indented `mermaid:` slot under `paths:`. | Deterministic, regen-from-source, auditable, diff-friendly. Replaces the placeholder PNG approach with a real diagram derivable from canonical CSVs. `mermaid.ink` HTTP endpoint is the offline fallback when `mmdc` is not installed. | **Approved** |
| **D-IH-4** | 2026-04-29 | **PDF tooling = WeasyPrint** (Python-only). Pandoc remains an optional escape hatch via `--format pdf` + manual install. | No TeX engine required; pip-installable; modern CSS support; opt-in via `requirements-export.txt` so the default install footprint is unchanged. The existing `--format pdf` code path falls back to a pandoc message when neither is available. | **Approved** |
| **D-IH-5** | 2026-04-29 | **GOI/POI taxonomy extension.** Add `client`, `partner`, `investor`, `regulator`, `vendor`, `lead`, `media` to the `class` enum recognised by `validate_goipoi_register.py`. Document program-onboarding sub-flow in the maintenance SOP. | Initiative 21 seeded six rows scoped to founder-incorporation; the registers are **engagement-keyed** (D-CH-8 from i21) so the dimension must accept all the entity classes that future programs will reference. Backwards-compatible: no existing-row impact. | **Approved** |
| **D-IH-6** | 2026-04-29 | **`git filter-repo` posture remains DEFERRED.** Initiative-21 UAT row C is updated to "DEFERRED — trigger not met". A `re-eval-trigger.md` template is added under `22-.../reports/`. | Per `SOP-HLK_TRANSCRIPT_REDACTION_001.md` §7, the trigger requires either a `restricted` POI in commit history without a public reference, or counsel issuing a privilege-protection demand. Neither has fired. `akos-governance-remediation.mdc` plus the workspace's commit-and-phase discipline section forbid force-push to `main` without explicit consent + trigger. | **Deferred (forward-only redaction continues)** |
| **D-IH-7** | 2026-04-29 | **Live Supabase mirror apply** = via `user-supabase` MCP `apply_migration` for the four Initiative-21 DDL files, then seed upserts via MCP `execute_sql` with `service_role`. Evidence captured in `reports/p7-supabase-apply-evidence.md`. | The DDL is already in `supabase/migrations/` with parity in `scripts/sql/i21_phase1_staging/`; this is the canonical promotion step. The MCP `apply_migration` is the operator-approved break-glass when CLI is not in scope; deny-`anon`/`authenticated` RLS protects the surface. Row-count probe (`SELECT count(*)`) confirms parity with CSV. | **Approved** |

## Re-evaluation triggers

- **D-IH-1**: Reopen for compliance-folder physical move when (a) two or more new planes add canonical CSVs, **or** (b) FINOPS adds three or more new registers. Track via `docs/wip/planning/README.md` row growth.
- **D-IH-6**: Reopen for `git filter-repo` when either trigger from `SOP-HLK_TRANSCRIPT_REDACTION_001.md` §7 fires. Initiative scope = scrub blobs in `delete-legal-transcripts/`; force-push window scheduled with operator and collaborators.
