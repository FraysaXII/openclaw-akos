---
language: en
status: active
intellectual_kind: phase_report
role_owner: System Owner
area: Tech / Holistik Ops
entity: Holistika Research
authority: Founder + System Owner
last_review: 2026-05-01
artifact_role: governed_evidence
topic_ids:
  - topic_holistik_ops_discovery
parent_topic: topic_holistik_ops_discovery
---

# I46 P1 — NEO4J_STRATEGY.md doctrine

**Phase:** P1 (write the missing doctrine in the v3.0 vault)
**Closes:** I46 P1 + evidence-matrix E1 (no doctrine page) + E5/E6 (GraphRAG framing) + E9 (KiRBe Neo4j separation only in I32 decision-log).
**Date:** 2026-05-01

## Actions

1. **Created vault sub-area** `docs/references/hlk/v3.0/Envoy Tech Lab/Neo4j/` (D-IH-46-F approved).

2. **Shipped canonical doctrine** [`NEO4J_STRATEGY.md`](../../../references/hlk/v3.0/Envoy%20Tech%20Lab/Neo4j/NEO4J_STRATEGY.md) (~225 lines):
   - 3-use-case framing table (A: Governance KG built; B: GraphRAG PoC; C: Agent memory deferred)
   - Use-case A section: what is, what it is FOR, what it is NOT for, doctrine for adding new node labels (4-step contract via `akos.hlk_graph_model`)
   - Use-case B section: library choice (D-IH-46-A: `neo4j-graphrag-python` LightRAG-style hybrid), rejected alternatives with rationale, decision-frame for ship vs no-ship, doctrine for adding GraphRAG to other skills
   - Use-case C section: why we don't have one, trigger conditions (operator picks one in P4 ADR), 2026 benchmark citation
   - Cross-repo posture table: AKOS / KiRBe / hlk-erp / boilerplate; D-IH-32-M restated permanent
   - Cost / performance envelope table: Aura free tier ceiling, full sync, GraphRAG indexing per Tianpan.co 2026 piece
   - Vendor lock-in posture: HLK CSV vault SSOT keeps everything portable
   - Operator-known gotchas: AKOS_API_KEY SPA fallback, Aura broken-chain TLS (D-IH-32-Q), password truncation (lesson from D-IH-32-Q)
   - Cross-references + 90-day review cadence

3. **Added cross-references** to 3 existing files (per master-roadmap success metric: cited by ≥3 other docs):
   - `prompts/overlays/OVERLAY_HLK_GRAPH.md` — added doctrine pointer at the top
   - `config/sync/kirbe-sync-contract.md` §"Things explicitly NOT changed" — restates D-IH-32-M with link to doctrine
   - `docs/references/hlk/v3.0/Envoy Tech Lab/Repositories/EXTERNAL_REPO_CONTRACT_TEMPLATE.md` — new optional "Neo4j posture" section pointing at doctrine

## Verification

- `validate_hlk.py`: PASS (152 files; new doctrine has `language: en` frontmatter and resolves all internal links)
- `validate_hlk_vault_links` (subset of `validate_hlk`): PASS
- Cross-reference count: 3 (OVERLAY + kirbe-sync §"NOT changed" + EXTERNAL_REPO_CONTRACT optional Neo4j section) — meets master-roadmap success criterion

## Architectural impact

The doctrine gap that accumulated across I7 → I23 → I25 → I32 is closed. Future "should we add this to Neo4j?" questions resolve to:
- **Use-case A** → does it have a canonical CSV? (4-step contract)
- **Use-case B** → is it MADEIRA-grounding? (PoC bar in P3+P5)
- **Use-case C** → has the trigger fired? (P4 ADR)

KiRBe Neo4j separation (D-IH-32-M) is now permanent doctrine in 2 surfaces (kirbe-sync-contract + NEO4J_STRATEGY) instead of buried in 1 decision-log.

External repos (KiRBe, hlk-erp, boilerplate) can now point at a canonical strategy doc when their teams ask "do you use Neo4j for X?" instead of getting an inconsistent verbal answer.

## What this does NOT do

- **No code changes** — doctrine only.
- **No new MCP tools** — that's P2.
- **No PoC** — that's P3.
- **No agent memory build** — D-IH-46-B defers; ADR in P4.

## Next phase

P2 — Use-case A hardening. New `hlk_graph_skill_neighbourhood` MCP tool (the first axis-6 traversal surface) + nightly drift canary + recurring idempotency proof.
