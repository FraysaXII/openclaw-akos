# I54 / P3 — Run a11y audit (DISPATCHER VALIDATION; OPS-54-1 forwarded)

**Date:** 2026-05-03
**Phase:** P3 (Run a11y audit)
**Outcome:** **dispatcher-validation only** (stub-mode); **OPS-54-1 forwarded** to next operator-side install + run.

## Why dispatcher-validation only

This cursor session does not have PyPI access to install
`axe-playwright-python` (the I54 P1 dev dependency). Per **R-54-4** the
documented response is a **stub-mode** audit pass that:

1. Verifies the wiring (CLI flag, helpers, fallback semantics, in-scope
   surface registration, DOM-source contract).
2. Reports zero findings as the dispatcher-validation outcome.
3. Forwards **OPS-54-1** to the next operator-side install + run, where
   the live audit overwrites the per-surface report's "Findings" table.

This pattern mirrors:
- I52 P3 calibration burn → "DISPATCHER VALIDATION ONLY" banner; OPS-52-1 forwarded for real-API run.
- I52 P5 endpoint cost probe → `--stub` fixture; OPS-52-2 forwarded for first real RunPod / Kalavai run.
- I53 P3 GraphRAG A/B → NO-FIRE governance event; OPS-53-1 forwarded for first operator-funded live run.

## Per-surface dispatcher-validation reports

- [`a11y-audit-madeira-control-2026-05-03.md`](./a11y-audit-madeira-control-2026-05-03.md) — `static/madeira_control.html` rendered at `/madeira/control`. Zero findings stub. Dispatcher-validation evidence drawn from 12 HTML-source assertions in `tests/test_madeira_control_a11y.py` (I49 P15) + 8 DOM-source contract tests in `tests/playwright/test_madeira_control_a11y_dom.py` (I54 P2) + 8 wiring tests for the I54 P1 axe helpers.
- [`a11y-audit-dossier-html-2026-05-03.md`](./a11y-audit-dossier-html-2026-05-03.md) — dossier HTML rendered by `akos.dossier.html_render.render_dossier_html`. Zero findings stub. Dispatcher-validation evidence drawn from 11 template-level + 2 conditional-render tests in `tests/playwright/test_dossier_html_a11y.py` (I54 P2). **Note:** the dossier HTML URL is not yet registered in `AXE_IN_SCOPE_SURFACES`; that registration lands at I54 P5 once a stable test-fixture URL/path is wired (the live operator-driven run will use `file://` against a freshly rendered file).

## Wiring verified

```text
py -m pytest tests/playwright/ -v
======================== 25 passed, 2 skipped in 0.25s ========================
```

```text
py scripts/browser-smoke.py --axe   # without --playwright
[SKIP] axe_madeira_control            --axe requires --playwright (Initiative 54 P1)
```

```text
py scripts/browser-smoke.py --playwright --axe   # without axe-playwright-python installed
# axe block correctly falls through; would emit:
# [SKIP] axe_madeira_control   axe-playwright-python not installed.
#                              Run: pip install -r requirements-dev.txt (Initiative 54 P1)
```

## OPS-54-1 (forwarded)

| Field | Value |
|:--|:--|
| ID | OPS-54-1 |
| Class | operator-driven dev-tooling install + first live a11y audit |
| Description | First live axe-core audit of `static/madeira_control.html` + dossier HTML output via `py scripts/browser-smoke.py --playwright --axe` after `pip install -r requirements-dev.txt` + `py -m playwright install chromium` |
| Trigger | Next operator-side dev-tooling install cycle |
| Owner | System Owner |
| Acceptance | Both `reports/a11y-audit-*-YYYY-MM-DD.md` reports overwritten with per-impact counts + per-rule violation tables. **G-54-1 evaluation:** zero Critical findings = pass; any Critical = G-54-1 fires (P4) and `static/madeira_control.html` (or dossier template) is edited to remediate. |
| Reversal | Audit reports preserved; new audit on next operator run. |

## Forward look

- **P4** runs a no-fire governance event for **G-54-1** because the audit ran in dispatcher-validation mode (zero findings ≠ live findings; G-54-1 does not fire on stub-mode results); decision-log row records this and re-arms G-54-1 for the OPS-54-1 cycle.
- **P5** wires `playwright_a11y_smoke` profile in `config/verification-profiles.json`; registers the dossier HTML URL in `AXE_IN_SCOPE_SURFACES` (or via a separate fixture-driven dossier-HTML scenario list).
- **P6** closure UAT: pytest sweep, dossier `--filter madeira` Section 8 Surface UX subsection re-emit, CHANGELOG, README row, WIP_DASHBOARD; flips I49 follow-up `OPS-49-craft-followups` complete.
