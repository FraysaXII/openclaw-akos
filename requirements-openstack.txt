# Optional — ShadowPC / OpenStack GPU (`gpu-shadow`, `akos/openstack_provider.py`).
# Install after core requirements:
#   pip install -r requirements.txt -r requirements-openstack.txt
openstacksdk>=4.0.0
