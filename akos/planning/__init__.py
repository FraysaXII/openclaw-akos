"""Planning workspace governance helpers (Initiative 59).

Subpackage hosts SSOT modules consumed by both per-initiative ``master-roadmap.md``
frontmatter validators AND the new ``INITIATIVE_REGISTRY.csv`` schema. Adding a
new value here propagates to both surfaces atomically.
"""
