pytest>=7.0
pydantic>=2.0
# Observability (optional -- telemetry features degrade gracefully without it)
langfuse>=2.0
# GPU infrastructure (optional -- RunPod provider degrades gracefully without it)
runpod>=1.7.0
# Control plane API (optional -- only needed for scripts/serve-api.py)
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0
# Custom AKOS MCP server (scripts/mcp_akos_server.py)
mcp>=1.0.0
# Optional: for browser-smoke.py Playwright mode (pip install playwright && playwright install chromium)
playwright>=1.40
