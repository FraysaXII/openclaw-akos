pytest>=7.0
pydantic>=2.0
# Observability (optional -- telemetry features degrade gracefully without it)
langfuse>=2.0
# GPU infrastructure (optional -- providers degrade gracefully without them)
runpod>=1.7.0
openstacksdk>=4.0.0
# Control plane API (optional -- only needed for scripts/serve-api.py)
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0
# Custom AKOS MCP servers (scripts/mcp_akos_server.py, scripts/finance_mcp_server.py)
mcp>=1.0.0
# Finance data (optional -- finance MCP degrades gracefully without it)
yfinance>=0.2.36
# Optional: for browser-smoke.py Playwright mode (pip install playwright && playwright install chromium)
playwright>=1.40
