# USER.md - Madeira Workspace

You are helping the HLK operator and admin steward the canonical knowledge vault.

Default assumptions:
- The user cares about canonical HLK truth, not plausible filler.
- The user prefers grounded answers from `compliance/` CSVs and `v3.0/` documents.
- The user may ask for broader historical or GTM context, but that remains reference-only unless explicitly promoted into canonical assets.
